# Original rules ledger

What the retail game is documented to do, what the shipped data proves,
and what we still do not know. This is the F00-B.2 counterpart to the
content inventory: the inventory counts *what files exist*, this ledger
records *what the original rules are* and how well each is evidenced.

Every fact carries one classification:

- **verified_original** — confirmed against authored data in the local
  retail install or by an existing verified path in this tree.
- **documented** — stated in documentation shipped with the game, but
  not (yet) confirmed against data or observed behavior.
- **inferred** — reasoned from filenames, data patterns or prior
  research; not stated anywhere authoritative.
- **designed** — a deliberate rust-mm2 policy, not an original claim.
- **unknown** — requires research; provisional behavior does not count
  as an answer.

Rules imported from MM1, MM3 or MM2Hook are never assumed; if a fact
comes from community knowledge it says so explicitly and stays at most
*inferred* until verified locally.

## Sources and method

| Source | What it is | How it was read |
| --- | --- | --- |
| `MM2HELP.HLP` | In-game help, 183 topics (cited as `help:<topic>`) | Decompiled locally with helpdeco (GPLv3, tool only — output not committed) |
| `Readme.rtf` | Retail readme, troubleshooting + MP notes (cited `readme:§`) | `textutil` |
| `Booklet.pdf` | Jewelcase booklet, controls/maps (cited `booklet:p`) | `pypdf` text extraction |
| `TROUBLE.RTF` | Crash-recovery troubleshooting doc | `textutil` |
| `data:<path>` | Authored files in the retail install | `mm2-inspect dump` / `inventory` through the read-only VFS |
| code docs | `docs/vehicle-handling.md`, `docs/research/*` | Prior verified/inferred work in this tree |

Retail install: `/Users/linus/coding/rust-mm2/retail`, enumerated
2026-09-20 (catalog fingerprint `fnv1a64:e91e6cd4b2ae30d9` at commit
`156b0cc`). Numbers below are reproducible with
`mm2-inspect inventory <install>` and `mm2-inspect dump <install> <path>`.

## Drivers, profiles and progression

| ID | Rule | Class | Source |
| --- | --- | --- | --- |
| DRV-1 | A driver profile accumulates race history; multiple drivers can share one install. | documented | help:Creating or Loading a Driver; data:`players/` (17 files) |
| DRV-2 | Each driver has a fixed rank: Amateur or Professional. | documented | help:Creating or Loading a Driver |
| DRV-3 | Amateur = longer Blitz times, less traffic, easier unlocks. Professional = shorter Blitz times, more traffic, harder unlocks, earns Pro points. | verified_original | help:Creating a New Driver; data:`mm*data.csv` — the second parameter block per row consistently carries shorter TimeLimits and higher densities/opponent counts |
| DRV-4 | Pro points are awarded on two criteria: finishing position and vehicle driven (slower vehicle, more points). Exact formula unknown (UNK-8). | documented | help:Creating or Loading a Driver |
| DRV-5 | Best results are recorded per driver per race and viewable on Race Records / Driver's Stats, filterable by race type, city, and Amateur Times / Pro Times / Pro Points. | documented | help:Race Records, help:Driver's Stats Screen |
| DRV-6 | Records are kept only for races run under default conditions; customizing laps/checkpoints/opponents excludes that run. | documented | help:Race Records |
| DRV-7 | The last remaining driver cannot be deleted. | documented | help:Main Menu Screen |
| DRV-8 | Quick Race jumps straight into the last race played after vehicle select. | documented | help:Quick Race, help:Main Menu Screen |

## Race modes

| ID | Rule | Class | Source |
| --- | --- | --- | --- |
| RACE-1 | Four competitive single-player modes exist — Blitz, Checkpoint, Circuit — plus free-roam Cruise, and the Crash Course schools. | verified_original | help:The Races; data:`race/<city>/` families + `mm*data.csv` |
| RACE-2 | Each event has fixed authored location, checkpoint set, weather, time-of-day and densities. | verified_original | help:Blitz/Checkpoint/Circuit; data:`mm*data.csv` per-row Weather/TimeofDay/Ambient/Peds/Cops |
| RACE-3 | Finishing top-3 as Amateur or 1st as Professional in a Blitz/Checkpoint/Circuit race unlocks customization of that race's weather, time-of-day, traffic/ped/cop density (Circuit: laps + opponents, no pedestrians). | documented | help:Blitz Race, help:Circuit Race, help:Unlocking Races, help:Races Screen |
| RACE-4 | Cruise-mode weather/time-of-day/density options are always available, no unlock needed. | documented | help:Races Screen |
| RACE-5 | Destroying your vehicle in a Blitz or Checkpoint race means restarting from the beginning; in a Circuit race it costs a time penalty and the vehicle resets. | documented | help:Blitz Race, help:Checkpoint Race, help:Circuit Race, help:Durability |
| RACE-6 | The green compass arrow points to the nearest un-cleared checkpoint and turns yellow when that checkpoint is behind you; X/S cycle the arrow through remaining checkpoints. | documented | help:Using Dashboard Instruments, help:General Keyboard Game Controls; booklet:p6 |
| RACE-7 | In Checkpoint races the finish line only appears after every checkpoint is cleared. | documented | help:Checkpoint Race, help:Tips for Winning |
| RACE-8 | Amateur rows: 4-7 opponents in Checkpoint/Circuit, 0 in Blitz; Professional rows: 4-7 (london checkpoint 6-7). | verified_original | data:`mmracedata.csv`, `mmcircuitdata.csv` |
| RACE-9 | Authored per-city selectable-event rosters: 12 Checkpoint, 10 Blitz, 10 Circuit rows in each city's `mm*data.csv`. | verified_original | `mm2-inspect inventory` event-metadata rows (both cities) |
| RACE-10 | More `.aimap` event files ship than the tables list (london race0-13/blitz0-12/circuit0-11, sf race0-11+r0/blitz0-13/circuit0-11 vs 12/10/10 rows). Whether the extra files are selectable events or dev leftovers is unknown (UNK-2). | verified_original | `mm2-inspect inventory` races family vs event-metadata rows |
| RACE-11 | `race/<city>/<stem>.aimap` binds the event's Amateur actor roster and `<stem>.aimap_p` the Professional one: `[Opponent]`/`[Police]` counts equal the corresponding `mmracedata.csv` block's Opponents/Cops on 23/24 checkpoint events. `sf/race0` is an authored anomaly — amateur Opponents=7 while `race0.aimap` wires 6 (`race0-a-6.opp` ships unreferenced). The `mm2-inspect opponents` audit (2026-09-21) re-measures the opponent half of this on *every* table kind: `sf/race0` amateur is still the only wired-vs-table count mismatch — every other event's `[Opponent]` rows match its authored count at both difficulties (Blitz wires 0 rows matching Opponents=0). | verified_original | measured cross-check `mmracedata.csv` vs all 24 checkpoint `race<N>.aimap{,_p}` (2026-09-20); `mm2-inspect opponents` all-table audit (2026-09-21); docs/research/aimap.md |
| RACE-12 | An `[Opponent]` row wires `geo <route>.opp <tail>`: the opponent's vehicle id (amateur rows field base cars — `vpbug`/`vpcoop`/`vpcab`/`vpauditt`…; professional rows shift to harder variants — `vpcoop2k`, `vpvwcup`, `vpdb7`, `vppanoz`, `vppanozgt`), an authored `.opp` driving line, and a numeric tail (10 values on race rows; decoded into the documented driving-parameter vocabulary — RACE-14/UNK-11). Many events ship spare `.opp` files no `[Opponent]` row references (43 london + 34 sf records, including `blitz3`/`blitz4` route files on 0-opponent events); non-table stems still carry lineups (`race/london/race12.aimap` wires 1; `race/sf/stunt0.aimap` wires 1 whose `opp-c0.2` is a dead ref). Every wired vehicle id resolves to a `ready` vehicle-catalog entry on retail. | verified_original | `mm2-inspect opponents` on retail (2026-09-21): 64 builds/city, 0 failed, 271 london + 246 sf opponents wired, 0 unresolved vehicle ids; docs/research/aimap.md |
| RACE-13 | `tune/vehicle/<id>_opp.vehcarsim` exists for nearly every stock car (23 files on retail) and is a *sparse override*, not a standalone tune: authored values differ from the base file (`vpbug_opp`: inertia box, drivetrain, end-train inertias, horsepower, top speed), but most `_opp` files omit fields the base file carries, and several (`vp4x4_opp`, `vpbus_opp`, `vpcab_opp`…) author transmission data in an alternate schema — `NumGears`/`GearRatios`/`UpshiftRPM`/`DownshiftRPM`/`DownshiftBias` — that the player files' `ManualNumGears`/`Low`/`High` band schema does not use. `vpsemi_opp` and `vpvwcup_opp` are complete under the base schema. Our `load_opponent` merges the `_opp` document over the base tune (authored fields win, the rest inherit) — a designed policy: the files' existence and contents are verified, their exact original consumption is not (UNK-11). | verified_original | `mm2-inspect list`/`dump` field audit of all 23 retail `_opp` files (2026-09-21) |
| RACE-14 | The ten-value `[Opponent]` tail decodes into the driving-parameter vocabulary the community format reference publishes (angel-file-formats `AIMAP.md`, R3): col 0 `maxThrottle` (0.57–1.00), col 1 an `unknown/unused` flag, col 2 look-ahead distance (50–150; `someDistancePadding` default 75), col 3 `cornerBrakingThreshold` (~0.7-centred, default 0.7 — R3: brake-demand floor), cols 4–8 flags `avoidTraffic`/`avoidProps`/`avoidPlayers`/`avoidOpponents`/`weirdPathfinding`, col 9 `cornerSpeedMultiplier` (0.89–2.29, professional rows reach 2.0+). The column *order* is **documented**: R3's table matches mm2hook's recovered `RegisterRoute` flag-argument order (1, 4–8) position-for-position, and every float column's retail range brackets the recovered default — superseding an earlier inferred order that had the flag block shifted one column late. The original's runtime *semantics* stay unverified (UNK-11). Our consumption is partial and disclosed: `maxThrottle` → a throttle ceiling, `cornerSpeedMultiplier` → the corner-brake floor, look-ahead → the corridor sensing reach (designed reading), `avoidPlayers`/`avoidOpponents` → gate human/AI participants in the corridor (documented polarity; retail authors `avoidPlayers=0` on 75% and `avoidOpponents=0` on 59% of rows); `avoidTraffic`/`avoidProps` inert (no such runtime classes), `unkFlag`/`cornerBrakingThreshold`/`weirdPathfinding` bound but unconsumed. | documented | angel-file-formats `AIMAP.md` + mm2hook `aiVehiclePhysics::RegisterRoute` signature/defaults + retail `mm2-inspect dump` distributions on 536 rows (2026-09-24 re-measure); docs/research/aimap.md |

## Blitz

| ID | Rule | Class | Source |
| --- | --- | --- | --- |
| BLZ-1 | Solo race against a countdown timer: clear all checkpoints in any order and reach the finish before time runs out. | documented | help:Blitz Race |
| BLZ-2 | No opponents and no police in Blitz events. | verified_original | data:`mmblitzdata.csv` — Opponents=0, Cops=0 in all 40 rows |
| BLZ-3 | Per-event time limits are authored (25-120 amateur, 18-103 professional on retail). Unit unverified (UNK-4). | verified_original | data:`mmblitzdata.csv` TimeLimit |
| BLZ-4 | Blitz has ambient traffic and pedestrians; per-event densities are authored (am 0.0-0.7/0.1-0.4; pro 0.2-0.9/0.1-0.5). | verified_original | data:`mmblitzdata.csv` Ambient/Peds |
| BLZ-5 | Running out of time before finishing fails the run; the run can be restarted. | inferred | follows from BLZ-1's "before time runs out"; help:Blitz Race — exact fail screen unverified |

## Checkpoint

| ID | Rule | Class | Source |
| --- | --- | --- | --- |
| CHK-1 | Race against opponents through all checkpoints in any order; finish line appears last. | documented | help:Checkpoint Race |
| CHK-2 | 12 Checkpoint races per city; only the first three are unlocked initially. | verified_original | help:Checkpoint Race ("all 12", "first three"); data:`mmracedata.csv` = 12 rows/city |
| CHK-3 | Placing top-3 (Amateur) or 1st (Professional) in each race of a set of three unlocks the next set. | documented | help:Checkpoint Race, help:Unlocking Races |
| CHK-4 | Police appear at fixed authored locations per race. | verified_original | help:Tips for Winning ("cops are located in specific places"); data:`mmracedata.csv` Cops 0-4 am / 0-8 pro |
| CHK-5 | Opponents may take different routes; each is shown on the map as a colored triangle. | documented | help:Tips for Winning, help:Displaying a Map of the City |

## Circuit

| ID | Rule | Class | Source |
| --- | --- | --- | --- |
| CIR-1 | Ordered checkpoints define a closed course; laps must beat the opponents. Missed checkpoints must be cleared or the lap does not count. | documented | help:Circuit Race |
| CIR-2 | Cross-streets are closed off; off-road shortcuts through parks etc. still possible. | documented | help:Circuit Race, help:Tips for Winning |
| CIR-3 | No pedestrians in Circuit races. | verified_original | help:Circuit Race; data:`mmcircuitdata.csv` Peds=0 in all rows |
| CIR-4 | No ambient traffic and no police in Circuit races. | verified_original | data:`mmcircuitdata.csv` Ambient=0, Cops=0 in all rows |
| CIR-5 | Lap counts are authored per event (2-3 amateur, 2-4 professional on retail). | verified_original | data:`mmcircuitdata.csv` NumLaps |

## Authored race records (waypoints, start grids)

|| ID | Rule | Class | Source |
|| --- | --- | --- | --- |
|| WPT-1 | `<stem>waypoints.csv` rows carry `x,y,z,a,w`; `w` behaves as the checkpoint trigger radius (half street width — retail values 8-15 m). | inferred | data:`race/*/…waypoints.csv`; used by `mm2_content::race_def` |
|| WPT-2 | Row roles for Blitz/Checkpoint: row 0 is the start line, rows 1..n-1 are any-order checkpoints, the last row is the finish trigger. For Circuit every row after 0 is an ordered gate and the course closes through the start line again. | inferred | consistent across retail rows (e.g. blitz0 = line + 3 gates + finish, matching BLZ-1/CHK-1/CIR-1 docs); measured on all 24 checkpoint waypoint files 2026-09-20 — every file has ≥3 rows and a last row distinct from row 0 (london race0: 7 rows, finish ~840 m from the start; london race9's finish is closest at ~49 m); provisional |
|| WPT-3 | `<stem>_strtpnts` files hold the starting grid (one `x,y,z,a` row per slot). Only SF circuits ship them, under the short stem `cir<N>` — `cir1_strtpnts` … `cir9_strtpnts` — while the event rows are `circuit1`…`circuit9`; the same-index alias is the catalog's inference. | inferred | data:`race/sf/cir*_strtpnts`; `mm2-inspect events` extras listing |
|| WPT-4 | The `a` columns are degrees in two different conventions, measured 2026-09-21: waypoint `a` is a course bearing (`atan2(dx,dz)` along the rows), while `_strtpnts` `a` and the `.opp` row-0 staging field are vehicle yaw — forward `(−sin a, −cos a)`, exactly 180° apart. `RaceStart.yaw_deg` stores vehicle yaw and `Quat::from_rotation_y(yaw_deg.to_radians())` consumes it directly; the no-grid fallback derives it from the row0→row1 tangent. An authored `_strtpnts` `a = 0` is *no heading* — the same zero-means-unset rule the `.opp` staging field uses (measured: `cir6_strtpnts` is the lone all-zero grid while its `.opp` routes stage ~180°, so a verbatim 0 faces the grid backward); `yaw_deg` is `Option` and a `None` consumer derives the course facing (`course_yaw` for the player, the route staged heading/first leg for opponents). Whether the original *enforces* gate direction from waypoint `a` stays open (UNK-16). | inferred | data: all 612 retail `.opp` + `cir*_strtpnts` vs waypoint geometry; `mm2_content::race_def`, `mm2_app::session` |
|| WPT-5 | All waypoint/start coordinates are in world space in the same frame as the PSDL city mesh. | verified_original | retail blitz0 waypoints coincide with the London street mesh the spawned car sits on (screenshot evidence) |

## Cruise

| ID | Rule | Class | Source |
| --- | --- | --- | --- |
| CRZ-1 | Free roam of either city, no clock, no opponents. | documented | help:Cruise, help:Races Screen |
| CRZ-2 | Ambient traffic, pedestrians and police are active; police chase on sight, and "flashy" cars attract more attention. | documented | help:Tips for Winning (Cruise tips) |
| CRZ-3 | Losing a pursuing cop only requires leaving its sight. | documented | help:Tips for Winning |

## Crash Course

| ID | Rule | Class | Source |
| --- | --- | --- | --- |
| CC-1 | Two schools: London Cabbie (East End Cab Company) and SF Stunt Driver (Golden Gate Stunt Driving School). | documented | help:Crash Course, help:Crash Course Screens |
| CC-2 | Each school: 9 lessons, 3 midterms, 1 final — 13 authored events, ordered lesson1-3 → midtrm1 → lesson4-6 → midtrm2 → lesson7-9 → midtrm3 → final. | verified_original | help:Crash Course ("nine lessons", "three lessons in a group"); data:`mmcrashdata.csv` 13 rows in that exact order; `crash0-12` files |
| CC-3 | Passing each group of three lessons unlocks that group's midterm; passing all three midterms unlocks the final. | documented | help:Crash Course |
| CC-4 | Required vehicle per school: Ford Mustang Fastback (SF), London Cab (London). Passed lessons can be replayed in any vehicle. | documented | help:Crash Course |
| CC-5 | "Work Experience" offers Blitz/Checkpoint races from the Crash Course screen. | documented | help:Crash Course |
| CC-6 | Rewards: SF midterms 1/2/3 → RSi Angel Cup paint / Double-Decker paint / Fastback paint, SF final → LTV; London midterms 1/2/3 → RSi Microsoft Cup paint / London Cab paint / Mini Cooper Classic paint, London final → Aston Martin DB7. | verified_original | help:Crash Course Rewards; data:`sf_rewards.csv`/`london_rewards.csv` — `crash,3/7/11` (the midterm row positions in the 13-row table) award `vpvwcup`/`vpddbus`/`vpbullet` paints (SF) or `vpvwcup`/`vpcab`/`vpcoop` paints (london), `crash,12` (the final row) awards `vp4x4`/`vpdb7` outright |

## Vehicle roster and unlocks

| ID | Rule | Class | Source |
| --- | --- | --- | --- |
| VEH-1 | Help lists 20 player vehicles: 12 unlocked + 8 locked. The shipped roster has 21 — `vpmoonrover` ("Moon Rover", 800hp, 1M durability) is undocumented (UNK-3). | verified_original | help:The Vehicles; data:`tune/*.info` + `EXPECTED_STOCK_ROSTER` |
| VEH-2 | Unlocked from the start: New Beetle, '68 Mustang Fastback, City Bus, London Cab, Eldorado, Freightliner Century, Mini Cooper Classic, Mustang Cruiser (police), Double-Decker Bus, F-350, Mustang GT, Panoz Roadster. | documented | help:The Vehicles |
| VEH-3 | Locked-vehicle unlock rules — Audi TT: top-3/1st in half the SF Checkpoint races; Panoz GTR-1: half the London Checkpoint races; Fire Truck: half the SF Blitz; Beetle Dune: half the SF Circuit; Beetle RSi: half the London Circuit; NEW MINI COOPER: half the London Blitz; LTV: finish SF Crash Course; DB7: finish London Crash Course. | verified_original | help:Unlocking Vehicles; data:`*_rewards.csv` — `{blitz,circuit,race},half` rows award exactly these cars on exactly these cities (`race` = the Checkpoint table), and the `crash,12` final rows award `vp4x4`/`vpdb7` (CC-6) |
| VEH-4 | Custom paint unlock rules — TT/Fire Truck/Dune/RSi(first)/NEW MINI: top-3/1st in *all* of the corresponding race set; Fastback/Cab/Mini Classic/DD Bus paints: specific midterms (CC-6); RSi Angel/Microsoft cups: SF/London midterm 1. | verified_original | help:Unlocking Custom Paint Jobs; data:`*_rewards.csv` — the `{family},all` rows award a nonzero `VariantNum` (a paint, per the authored header) on the same car the `half` row unlocks; the midterm/final paints are CC-6's indexed `crash,N` rows. `VariantNum` = the zero-based `Colors`/paint-job index, measured (2026-09-21): vpvwcup's 5/6 land on "Team Angel"/"Team MS" — the documented Angel/Microsoft Cup paints |
| VEH-5 | `.info` files carry `UnlockScore` (only `vppanozgt` nonzero at 8000), `UnlockFlags`, `Flags`, `ScoringBias` fields. Bit/score semantics are inferred at best — `UnlockScore=8000` likely ties to Pro points but is unverified (UNK-6). `UnlockFlags` is *not* the select-roster lock: it is nonzero on `vpbus`/`vpbullet`/`vpcentury`/`vpcop`/`vpsemi`/`vppanozgt` (plus the unlisted `vpeagle`) but zero on six cars the reward tables verifiably lock (`vpauditt`, `vpdune`, `vpvwcup`, `vpcoop2k`, `vp4x4`, `vpdb7`) — the authored rewards are the progression authority (DSN-18). | verified_original | data:`tune/vp*.info` cross-checked vs `*_rewards.csv` (2026-09-21) |
| VEH-6 | Vehicle stats shown to the player: Horsepower, Top Speed, Durability, Mass. | verified_original | help:Key Properties; data:`tune/*.info` fields |
| VEH-7 | Durability covers collision resistance and rough-terrain abuse; high-clearance trucks survive stairs that wreck low cars. | documented | help:Key Properties |
| VEH-8 | The Police Car is usable in Cops vs. Robbers even while locked, when playing the cop side. | documented | help:Ford Mustang Cruiser |

## Damage and recovery

| ID | Rule | Class | Source |
| --- | --- | --- | --- |
| DMG-1 | A Damage Meter HUD bar runs green → yellow → red; at zero the vehicle stops working. | documented | help:Using Dashboard Instruments |
| DMG-2 | Destruction in Blitz/Checkpoint = restart race; in Circuit = time penalty + reset (RACE-5). | documented | help:Durability |
| DMG-3 | Vehicles have "more detailed damage modeling and breakaway parts". | documented | booklet:p3 |
| DMG-4 | In Cops & Robbers, damaged vehicles not carrying gold heal over time. | documented | help:Cops & Robbers |
| DMG-5 | `.vehcardamage` tune files exist per vehicle and decode to a typed model: `MaxDamage`/`MedDamage`/`ImpactThreshold`/`RegenerateRate`/`TextelDamageRadius`, two smoke pivots (`SmokeOffset`/`SmokeOffset2` + `DoublePivot`/`MirrorPivot`) and a flat embedded particle spec sharing the `dgBangerData` `BirthRule` vocabulary. 20 retail records, uniform 38-field set; `MirrorPivot` adds a 39th field on 7 (vpbullet, vpbus, vpcaddie, vpcop, vpddbus, vpdune, vpmustang99 — all authored 0). Whether/how the original consumes each field is unverified (UNK-13). | verified_original | data:`tune/vehicle/*.vehcardamage`; `mm2-inspect damage` + `dump` retail census 2026-09-22; docs/research/damage.md |
| DMG-6 | Authored damage bounds: `ImpactThreshold` 1500 on 19 of 20 records (100 on vpcaddie), `RegenerateRate` 0 on every record, `MedDamage < MaxDamage` on every record; `MaxDamage` spans 187.5k (vpcoop/vpcoop2k)–3.28M (vpsemi) tracking vehicle mass (impulse-scale, inferred); `TextelDamageRadius` spans 0.4 (vpcaddie/vpcentury)–20.0 (vp4x4/vppanozgt); `DoublePivot` is 1 on vpddbus/vppanoz/vppanozgt, 0 elsewhere. `vpmoonrover` ships no damage record — an authored absence, not an install gap. | verified_original | `mm2-inspect damage` + `dump` retail census 2026-09-22 |
| DMG-7 | `.vehstuck` records exist per vehicle: `Turn`/`Rotation`/`Translation`/`TimeThresh`/`PosThresh`/`MoveThresh` (20 files, uniform set) — the authored stuck-detection thresholds. Values: `Turn` ≈ π on 10 of 20 (vpford 3.098593), 1.57 on 6, others on vpbus/vpcentury/vpddbus/vpsemi; `Rotation` 0 on all; `Translation` ≈ 0.1 (vpcoop 0.164); `TimeThresh` 2.0 on 6, ~1.0 elsewhere; `PosThresh` 1.25 and `MoveThresh` 1.75 uniform — MoveThresh exceeds PosThresh on every record. MM2Hook's recovered `vehStuck` struct (2026-09-22) confirms the detector shape — `m_State`, `m_StuckTime`, `m_LastImpactPos`, squared thresh copies — while its `Update()` body stays unrecovered, so the implemented test combination is designed (DSN-20, UNK-13). | verified_original | data:`tune/vehicle/*.vehstuck`; `mm2-inspect damage` + `dump` 2026-09-22; MM2Hook `src/modules/vehicle/stuck.h` |
| DMG-8 | `.vehgyro` records exist per vehicle: `Drift`/`Spin180`/`Reverse180` always, `Roll`/`Pitch` on 17 of 21 (absent on vpbug, vpcab, vpford, vpvwcup_angel; 0.0 where present) — the authored spin/drift/righting rates. MM2Hook's recovered `vehGyro` carries the same fields plus `Spinable`/`Driftable`/`Rightable` asNode gates; its `Update()` is unrecovered, so application semantics stay unverified (a designed reading is implemented, DSN-22, UNK-13). | verified_original | data:`tune/vehicle/*.vehgyro`; `mm2-inspect damage` + `dump` 2026-09-22; MM2Hook `src/modules/vehicle/gyro.h` |
| DMG-9 | Vehicle body shaders carry damage-state texture pairs: a PKG shader naming `<stem>_dmg` is the damaged variant of `<stem>`. MM2Hook's recovered `fxTexelDamage::Init` (a `vehCarModel` member fed by `vehCarDamage`'s `TextelDamageRadius`/`ImpactsTable`, DMG-5/UNK-13) strips the suffix at the last `_` case-insensitively and binds the clean texture — the `_dmg` original is stashed per shader slot and its texels are blitted onto a cloned live texture at impacts; a failed clean lookup keeps the `_dmg` name. Observed on retail: damage-region body sections are *authored* bound to `_dmg` shaders — `vpbug` BODY_H sections 4–6 (the car's whole right half, `x>0`) name `vpbugyellow_{sd,ft,bk}_dmg`; 22 of 27 base `vp*.pkg` (excluding `_dash`/`_trailer` variants) ship `_dmg`-bound BODY sections (vpbus 12 sections/6 slots; vpcop, vpdb731, vpmoonrover, vpmustang99, vpvw_dune none). `ApplyDamage`'s radius/barycentric mechanics and `Reset` are recovered and implemented (F05-B.9, `mm2_game::texel`/`mm2_app::texel_fx`); the splat shape (`ApplyBirdPoopDamage`) stays unrecovered — a designed radial blit (DSN-32). | verified_original | MM2Hook `src/modules/effects/texeldamage.{h,cpp}` + `src/modules/vehicle/carmodel.{h,cpp}`; `mm2-inspect dump` section/shader census of retail `geometry/vp*.pkg` 2026-09-23 |
| REC-1 | Player vehicles carry authored breakaway parts: `BREAK<NN>` chunks inside `geometry/<id>.pkg`, bound to `geometry/<id>_break<NN>.mtx` transforms and `tune/banger/<id>_break<NN>.dgbangerdata` fragment records (the banger `<base>_break<N>` convention, WLD-15). Naming reads as corner pieces (`BREAK0`–`BREAK3`) plus edge panels (`BREAK01/12/23/03`) — inferred. 14 catalog vehicles ship parts; vpsemi/vpftruck/vplafrance the largest sets (6 pieces each). Retail also ships 10 dead fragment records (vpeagle `break0/1` with no pkg at all, vpvw_cup/vpvwcup `break01/02`, all four vpvwcup_angel). Detachment rules — damage threshold vs impact — unverified (UNK-13). | verified_original | `mm2-inspect damage` + `mm2-inspect banger` retail census 2026-09-22; docs/research/damage.md |

## Police (single player)

| ID | Rule | Class | Source |
| --- | --- | --- | --- |
| COP-1 | Police chase on sight in modes where they are enabled; count/locations are per-event authored data (RACE-2, CHK-4). | verified_original | help:Tips for Winning; data:`mm*data.csv` Cops |
| COP-2 | Escaping = leaving the cop's sight; conspicuous cars get reported ahead and chased on sight elsewhere. | documented | help:Tips for Winning |
| COP-3 | The Police Car (`vpcop`) is a distinct roster entry with `_cop` tuning variants present in data. | verified_original | data:`tune/vpcop.info`, `tune/vehicle/*_cop.*` |
| COP-4 | Pursuit AI, ram tactics, wanted-level model: nothing in shipped docs or obvious data describes it. | unknown | UNK-9 |

## Multiplayer

| ID | Rule | Class | Source |
| --- | --- | --- | --- |
| MP-1 | Up to 8 players total ("seven other people") via MSN Zone, IPX or TCP/IP; serial/modem are 1v1. | documented | help:Types of Multiplayer Connections, help:Multiplayer Screen |
| MP-2 | Max 10 concurrent hosted sessions per LAN. | documented | readme:§7.05 |
| MP-3 | Host controls connection type, password, max players, event, location, conditions, difficulty (from host's driver rank), and which vehicles/checkpoint races are unlocked — the host's record applies to everyone. | documented | help:Hosting vs. Joining |
| MP-4 | Modes: Cruise, Blitz, Checkpoint, Circuit, Cops & Robbers. No ambient traffic, cops or AI opponents in MP races; humans replace AI opponents. | documented | help:Multiplayer Games |
| MP-5 | Race joiners must be in before the host starts; Cruise and C&R allow join/leave at any time. A leaver's vehicle disappears for everyone; host disconnect → a new host is designated. | documented | help:Multiplayer Games |
| MP-6 | No pausing in multiplayer (ESC still opens the menu). | documented | help:Pausing and Resuming, help:Displaying a Map |
| MP-7 | Host can eject players via F6 (in-game player list) or the lobby Eject button. | documented | help:Multiplayer Games, help:Multiplayer Lobby Screen |
| MP-8 | Lobby has chat, per-player vehicle/color, team pick (team modes), Ready (joiner) / Start Server (host), Game Settings box. | documented | help:Multiplayer Lobby Screen |
| MP-9 | MP-specific data is thin: `copchase`/`multicop`/`*_p` records exist but no C&R-specific data discovered. The 99 `.aimap_p` variants once suspected as MP files are evidenced as Professional-difficulty actor rosters instead (RACE-11). | verified_original | `mm2-inspect inventory` multiplayer family; RACE-11 cross-check |

## Cops & Robbers

| ID | Rule | Class | Source |
| --- | --- | --- | --- |
| CNR-1 | A gold bar spawns somewhere in the city; deliver it to your hideout to score, then another appears; carriers can be rammed to drop the gold, which anyone can pick up by driving over it. | documented | help:Cops & Robbers |
| CNR-2 | Three variants: Free-for-all (any unlocked vehicle, own hideout), Cops vs. Robbers (cops = Mustang Cruiser → bank; robbers = Mustang GT → hideout), Robbers vs. Robbers (teams, any unlocked vehicle, several hideouts). | documented | help:Cops & Robbers, help:Host Settings Screen |
| CNR-3 | Host options: location, weather, time-of-day, pedestrian density, match limit (none / time / points), gold mass (weightless / ¼ ton / ½ ton — heavier slows the carrier and holds the gold better). | documented | help:Cops & Robbers, help:Host Settings Screen |
| CNR-4 | Non-carriers heal over time (DMG-4). Results show individual points; team variants also show team totals. | documented | help:Cops & Robbers |
| CNR-5 | Gold/hideout/bank positions, respawn rules, scoring values, drop physics: not in shipped docs or identified data. | unknown | UNK-10 |

## HUD, map and cameras

| ID | Rule | Class | Source |
| --- | --- | --- | --- |
| HUD-1 | Dashboard instruments: speedometer, tachometer (shift near redline on manual), gear display (R N 1 2 3 4…), damage meter, steering bar (mouse control only). | documented | help:Using Dashboard Instruments |
| HUD-2 | Race instruments: green compass arrow (Checkpoint/Blitz), checkpoint list, laps record (Circuit), place indicator, stopwatch (Checkpoint/Circuit), countdown timer (Blitz). | documented | help:Using Dashboard Instruments |
| HUD-3 | Cameras: Chase Near (default), Cockpit, Chase Far on C. W toggles wide-screen; V = "Thrill Cam"; cockpit look left/right/back/forward on numpad 4/6/2/8; BACKSPACE rearview mirror; D dashboard; H HUD; I opponent indicator. | documented | help:Camera Views, booklet:p9 |
| HUD-4 | Map: TAB cycles two smaller map views/off, E zooms, F toggles rotation, Q = full-screen pause map (single player only). Shows player, opponent triangles, un-cleared (bright) / cleared (dark) checkpoints, yellow highlight on arrow target, finish line when unlocked. | documented | help:Displaying a Map of the City |
| HUD-5 | London tunnels are marked on the map; some alleys/shortcuts are not. | documented | help:Tips for Winning |

## Controls and options

| ID | Rule | Class | Source |
| --- | --- | --- | --- |
| CTL-1 | Keyboard defaults: arrows drive (down = brake then auto-reverse when stopped), SPACE handbrake, ENTER horn, T auto/manual, R reverse/drive toggle (only from/to 1st), A/Z shift up/down, C camera, TAB map, Q full map, E zoom, F rotate map, W wide view, H HUD, I opponent arrows, V thrill cam, D dash, BACKSPACE mirror, F1 controls list, F4 restart race, ESC menu, X/S cycle arrow target, 2-5 CD player. | documented | help:General Keyboard Game Controls, booklet:p8-9 |
| CTL-2 | Primary driving controller is selectable: keyboard, mouse (steer by moving, LMB throttle, RMB brake/reverse), joystick, game pad, steering wheel; joystick wins by default if installed, else mouse. | documented | help:Driving with a Mouse/Keyboard/Joystick |
| CTL-3 | Control Options: Auto Reverse, POV-hat look, Force Feedback, controller select, steering sensitivity, dead zone, calibrate, collision/road-force intensity, full control rebinding (with conflict warning), defaults. | documented | help:Control Options Screen |
| CTL-4 | Graphics Options: Textured Sky, Vehicle Reflections, Show Pedestrians, Display device, Renderer (auto-detected; Software Only fallback), Resolution (default 640x480), Visibility + Lighting Quality sliders, Texture Quality + Object Detail (Very High/High/Medium/Low), Cloud Shadows (High/Low/None), defaults. | documented | help:Graphics Options Screen, readme:§5 |
| CTL-5 | Audio Options: Sound FX, Play Commentary, Play Music, City Sounds, device select, Stereo FX (mono/stereo), Sound Quality (High = 16-bit 22kHz 32ch, Medium = 8-bit 22kHz 16ch, Low = 8-bit 11kHz 8ch), FX and Music/City volumes, balance, defaults. | documented | help:Audio Options Screen, help:Adjusting Volume |
| CTL-6 | CD player plays the game CD or any audio CD during play; keys 2/3/4/5. | documented | help:Playing Music While You Race |
| CTL-7 | `-nomipmap` command-line switch exists; Safe Mode / Redetect Video / No Movie / No Sound helper shortcuts ship in the install. | verified_original | readme:§3.19; install dir listing |
| CTL-8 | F4 restarts the current race; ESC opens the In-Game Menu (Quit to Race Menu / Options / Resume / Exit to Windows). | documented | help:Quitting a Race, help:General Keyboard Game Controls |

## Menus and flow

| ID | Rule | Class | Source |
| --- | --- | --- | --- |
| UI-1 | Main Menu: Crash Course, Races, Multiplayer, Quick Race, driver select/create/delete, Driver's Stats, Race Records, Options. | documented | help:Main Menu Screen |
| UI-2 | Races screen gates Time-of-Day/Weather/Ped/Traffic/Cop density (and Circuit laps/opponents) behind top-3 Amateur / 1st Professional finishes — always open in Cruise. | documented | help:Races Screen |
| UI-3 | Select Vehicle shows LOCKED overlays on locked vehicles and locked paint colors, plus transmission choice and the four stats. | documented | help:Select Vehicle Screen |
| UI-4 | Every screen has Options (top-right) and Help ("?"); ESC returns toward Main Menu. | documented | booklet:p2, help:Quick Race |
| UI-5 | A Results screen follows each race/match (placing + total time; C&R adds points). | documented | help:Checkpoint Race, help:Cops & Robbers |

## World and ambient behavior

| ID | Rule | Class | Source |
| --- | --- | --- | --- |
| WLD-1 | Traffic/ped densities are per-event authored fractions (0.0-0.9) and cop counts authored integers, all selectable post-unlock. | verified_original | data:`mm*data.csv` Ambient/Peds/Cops |
| WLD-2 | Pedestrians exist on streets and react audibly (screaming "Maniac!"); can be toggled via Show Pedestrians. | documented | help:Configuring Sound, help:Graphics Options |
| WLD-3 | Documented original defect: vehicles can stick in soft ground under SF dock archways; less likely at speed. | documented | readme:§3.05 |
| WLD-4 | Weather and time-of-day are per-event selectors (0-3 each in data; enum meaning unverified — UNK-1). | verified_original | data:`mm*data.csv` Weather/TimeofDay columns |
| WLD-5 | Sidewalks, alleys, parks, wrong side of road are all drivable shortcuts. | documented | help:Tips for Winning |
| WLD-6 | `.aimap` override grammar is uniform across all 209 retail files: `[Speed Limit]` scalar, counted `[Exceptions]` (road-id/density/speed), `[Police]`/`[Opponent]` spawns, `[Ambient Types/Density]` cumulative weights closing at 1.0, `[Ambients Drive On The Left]` (1 london / 0 sf), ped-model pairs; rarer `[Density]`, `[CopChaseDistance]`, `[AmbientLaneChanges]`, `[Traffic Lights]`, `[Hookmen]`. | verified_original | data: all `city/`+`race/` aimaps, `mm2-inspect aimap`; docs/research/aimap.md |
| WLD-7 | Retail exception density/speed values are all `0.00`/`0` — consistent with R3's claim that event aimaps close course roads to ambient traffic. Runtime consumption of these values is unverified (UNK-12). | inferred | data: every `[Exceptions]` row on retail |
| WLD-8 | Eight london `.aimap` files carry `[Exceptions]` road ids 562–815, outside `city/london.bai`'s 540-road space; SF exceptions are all in range. Different id space or dead authored refs — unknown (UNK-18). | verified_original | `mm2-inspect aimap` cross-check vs `city/london.bai` |
| WLD-9 | BAI section frames carry `x_axis` = `tangent × up` (the geometric right of travel with the sections) on ~all sections of both retail cities; right-side lane curves sit mostly at +x, left-side at −x — exceptions are authored geometry, so lane rank comes from measured offset, not side slot. | verified_original | measured on `city/{sf,london}.bai`; docs/research/bai.md |
| WLD-10 | Ambient turn rules: innermost lane turns toward centre or straight, outermost kerb-side or straight, middle lanes straight; one-way roads may take any outgoing road; freeway ramps may force a right; U-turns never. Intersection road lists are authored counterclockwise. London's left-hand driving is baked into authored BAI data (lane swap + reversed vertex/lane order), not a runtime flag. | documented | Adzima GDMag ambient-AI article; docs/research/bai.md |
| WLD-11 | Each retail city's vehicle road graph is one connected component (SF has exactly one dead end); 166/540 london and 96/379 sf roads are one-way; 176 roads carry no routable vehicle lanes (pedestrian/special/disabled or curve-less). Directed reachability (`mm2-inspect nav --routes`, 2026-09-20): london's graph is strongly connected — all 606 arcs reach all others. SF's is not: beyond the one dead end (road 0 backward), six arcs reach junctions no arm legally departs — authored one-way traps (roads 92/94/97/99 reach only themselves, 102/111 reach 2, road 1 backward reaches 617 of 618) — leaving 4927 ordered arc pairs unreachable (~1.3%); 505/512 seeded route probes succeed, the 7 failures are exactly the trap-region sources. Event `[Exceptions]` bind at routing time: `race/london/blitz0.aimap`'s 8 closures isolate roads 115-/116-/368+/375+ (15016 unreachable pairs, 24/512 probes unreachable — e.g. `270→108` routes 19 steps/1428 m open, `Unreachable` closed) and `race/sf/blitz0.aimap`'s 3 raise unreachable pairs to 7977 (11/512 probes); no probe ever traverses a closed road mid-route. | verified_original | `mm2-inspect nav --routes/--aimap` on retail |
| WLD-12 | `PTH1` pathset grammar is uniform across all 98 parseable retail files: header cursor + named paths of attributed points, kinds 0 single-points / 1 directed-pairs / 2 line-strip, spacing authored in quarter metres (0–60 m range seen; 0 and 5 m dominate). Placement names resolve to `geometry/<n>.pkg` (props) or `texture/<n>.*` (decals); `PATHnn` names are route labels (audio paths, parked-car/ferry/train routes). | verified_original | data: all 101 `.pathset` files, `mm2-inspect pathset`; docs/research/pathset.md |
| WLD-13 | `city/<city>/props.pathset` is the ambient prop-dressing source: london stamps 1188 prop instances from 87 paths (0 unresolved), sf 925 from 113 paths — its remaining 31 paths are `r4i_rails_f` *decal* names living inside the prop file, classified separately not failed. `props.pathset` names are prop PKGs; decal/audio/race pathsets are different consumers. | inferred | `mm2 --city {london,sf}` import reports + rendered stamp rows; docs/research/pathset.md |
| WLD-14 | The PSDL `prop_rule` byte selects a `n{NN}left`/`n{NN}right` row pair in `city/<city>/proprules.csv`: london bytes 1–16 ↔ rules n01–n16, sf 1–20 ↔ n01–n20 (n14 defined but unreferenced); 0 = no roadside props. `propdefs.csv` rows name 1–4 PKG variants plus start/distance/maxUse/minLerp/maxLerp fields; `props.csv` is a `Group,Name` membership list (retail: `Races` only). Each city has exactly one room carrying byte 205 with no matching rules — an authored anomaly, not a parser artifact. | verified_original | `mm2-inspect proprules` on retail; docs/research/proprules.md |
| WLD-15 | `tune/banger/*.dgbangerdata` is one `dgBangerData` record per banger stem (999 on retail, all `type: a`), carrying Size/CG/Mass/Elasticity/Friction/ImpulseLimit2/NumParts plus a `BirthRule` particle spec. The stem→geometry link is by name alone: `geometry/<stem>.pkg` (standalone prop, 216), `geometry/<stem>.mtx` or a matching PKG chunk (named part, 477), `<base>_break<NN>` → the `BREAK<NN>` chunk inside `geometry/<base>.pkg` (254 resolved); `default.dgbangerdata` is the fallback record. On every standalone prop, `NumParts` equals the PKG's distinct `BREAK<NN>` index count (0 mismatches). 47 authored records resolve to no geometry at all. | verified_original | `mm2-inspect banger` on retail; docs/research/banger.md |
| WLD-16 | World bangers bind by placed name (`N` → `tune/banger/<N>.dgbangerdata`) and reach the world through pathset stamping (`props.pathset` 17/17 london, 30/30 sf prop names bound; race overlays all bound) and the prop-rule channel (PSDL `prop_rule` → `proprules.csv` → `propdefs.csv` files, all bound). INST is the static-architecture channel: 0/386 distinct names bound. `*_ai.inst` supplements stamp only bound `sp_stop_f`. Vehicle (`vp*`/`va*`) records bind through the vehicle pipeline — 269/994 records reachable via world placements, 562 vehicle-owned, 163 authored-but-never-placed on retail. | verified_original | `mm2-inspect banger-bind` on retail; docs/research/banger.md |
| WLD-17 | Prop-rule PSDL geometry: a `Psdl::paths` record is one road run between two junction crossings and `road_rooms` chains the road rooms it traverses, consecutive entries sharing direct road-to-road boundaries. `start_crossroads`/`end_crossroads` name the curb vertex pair of the end crossings; a crossing occupies four consecutive perimeter points `[outer, curb, curb, outer]`, and at a direct road-to-road boundary the curb pair is the widest consecutive point pair marked with the neighbouring room id. The two arcs between the entry/exit crossings are the sidewalk building lines. Every rule-bearing room a sane path reaches resolves its crossings under this model (0 misses, both cities); some sf `road_rooms` entries encode non-room values (65 0xx range). | verified_original | measured on `city/{sf,london}.psdl` + `mm2_game::props` runtime walk; docs/research/proprules.md |
| WLD-18 | Decal `LineStrip` paths interleave two authored ribbon edges: even point indices form one edge, odd the other, each (even, odd) pair is a cross-section carrying the strip's width, and consecutive sections join into textured quads. Verified against retail: the pairing produces coherent authored widths — ~1 m zigzag (`decal_zigzag_l`), ~2 m cable-car rail channels (`r4i_rails_f`), ~8 m zebra crossings (`decal_rxwalk03_l`), 15–20 m junction paint (`decal_x_inter_l`) — where a centreline reading yields incoherent alternating ~1 m/~20 m segments; texture contents corroborate the axis (zigzag line oscillates along texture V, zebra bars run along U). Palette alpha in decal P8 textures is authored translucency (unpainted surrounds ≈ 36, painted bars ≈ 240). The exact UV policy (v tiling period vs stretch, u direction) and the original's blend state remain inferred. | verified_original | measured on `city/{sf,london}/decals.pathset` + `texture/decal_*.tex`; docs/research/pathset.md |
| WLD-19 | Surface materials live in one global pair `city/materials.{csv,mtl}` shared by both cities (no per-city copies, no other `.mtl` in the VFS). `materials.csv` maps texture stems to physics-material names (`texture,physics`; 3423 rows: 137 named, 3286 `none` = "no named material"); `materials.mtl` defines 8 `mtl <name> { … }` blocks (`deepwater`, `_default`, `grass`, `water`, `dirt`, `sand`, `cobblestone`, `wood`) each carrying the same ten fields — `elasticity`, `friction`, `effect` (`none` everywhere), `sound` (0/1/2 classes), `drag`, `width`/`height`/`depth`, `ptxindex`, `ptxthreshold`. PSDL texture-table names look up through the map with two normalizations: `<stem>-NNNN` animated-frame entries (`s_thames-0009`) fall back to the base stem (`s_thames` → `deepwater`), and 6 authored blank slots per city expect no coverage. Coverage: london 469 names = 152 named + 308 `none` + 6 blank + 3 unmapped (`sliver`, `sf_win_brickyel01_2s_4_l`, `sf_base_tan09_1s_5_l`); sf 457 = 148 + 301 + 6 + 2 (`sliver`, `gw_stc_offwhite_marswin_f`). Two dead authored refs: `transbay_ramp_f→ash`, `s_grass2mud→mud`; `_default`/`dirt`/`wood` are defined but referenced by no row. `none` does not mean non-solid — collision is a separate attribute. `city/<city>/{floors,walls}.csv` is a different neighborhood-grouping table, not this map. | verified_original | `mm2-inspect materials` on retail; docs/research/materials.md |
| WLD-20 | Ambient traffic vehicles are a distinct `va_*` class tuned by `tune/vehicle/<id>.aivehicledata` — an `aiVehicleData` block carrying Mass, Size, MaxAng, Elasticity, Friction, MaxDamage, PtxThresh, Spring, Damping, Limit, RubberSpring, RubberDamp (CG absent on 3 records; `va_garbagetruck` authors an MSVC `1.#QNAN0` MaxAng literal). 23 records ship in the shared pool; each city's `[Ambient Types/Density]` picks a subset as a cumulative-weight table closing at 1.0 — sf 11 rows/11 ids, london 13 rows/12 ids (`va_compact_s` legitimately occupies two weight bands). `race/london/roam.aimap{,_p}` and `roambak.aimap` author their own 12-row rosters — event-level replacement exists. `[Density]` per-event authors an ambient fraction (0.0 on `sf/collide0` = off; 0.05–0.10 on others) beside the `mm*data.csv` `Ambient` column. Row shape is `name cumulative-weight flag` with flag 0 (omitted once) on retail; flag/weight semantics and the original pool size remain unverified (UNK-12). | verified_original | `mm2-inspect traffic` on retail; docs/research/aimap.md |
| WLD-21 | Each city authors a 16-preset lighting grid `city/<stem>.lt00`–`.lt15` in the shared tune grammar: `NN = tod×4 + weather` with tod = morning/noon/evening/night and weather = clear/cloudy/foggy/rainy (measured: every record's `<weather>-<tod>` block name classifies back to its own file slot on all 32 retail records). Each record is MM2Hook's recovered `cityTimeWeatherLighting`: `Key`/`Fill1`/`Fill2` directional lights (`Heading`/`Pitch` radians + RGB `Color`) plus a BGRA-packed `Ambient` i32. This measures the `Weather`/`TimeofDay` column order half of UNK-1. The presets now bind to Bevy session lighting at the authored scales of authorship — slot map, directions (R4 `setLightDirectionInv`), colours, ambient — with designed absolute intensity constants (DSN-28). | verified_original | `mm2-inspect weather` on retail (105/105 parsed); MM2Hook `src/modules/city/citylevel.cpp`; docs/research/environment.md |
| WLD-22 | Each city's sky dome is `city/<stem>.sky` — one text line `<model> <hatY> <yMul> <rot>` naming a `geometry/<model>.pkg` dome mesh (`sky_dome_l`/`sky_dome`; the field names are R4-recovered `lvlSky` members). The shared `city/amb_<w><t>_<v>.ldef` grid (w ∈ c,f,p,r; t ∈ a,d,m,n; v ∈ f,l — 32 files) pairs name-for-name with `texture/sky_<grid>.tex` (32/32 on retail — inferred pairing); `.ldef` rows are a dev-machine bake-source `.tif` path plus two signed integer pairs, semantics unrecovered (UNK-24). | verified_original | `mm2-inspect weather` on retail; MM2Hook `lvlSky`; docs/research/environment.md |
| WLD-23 | The room-visibility table is `city/<stem>.cpvs` — binary `PVS0`, `index_count−1` RLE lists indexed by a stored end-offset table, 2-bit-per-room codes (room `r` at byte `r>>2`, bits `2*(r&3)` — the mm2hook `IsRoomVisible` layout, verified by 1340/1341 London self-visibility; the doc-derived 2-bit-offset reading was measured wrong). `lists == psdl.rooms + 1` on both base tables; retail uses only codes 0/3. The exe's `cityLevel::Load` opens exactly `city/<stem>.cpvs` (single `Open("city", stem, "cpvs")`, no numbered-name construction — verified in Midtown2.exe), so the 23 `<stem>_N`/dated extras are offline bake artifacts: `_8`/`london_bad` are byte-identical to base and the numbered files nest `_0 ⊋ _2 ⊋ _4 ⊋ _8=base ⊋ _16…_255` (larger N = stricter occlusion; `_00`/`_254` independent, `sf082100` an earlier-bake subset) — the shipped table is the N=8 bake. `city/<stem>.pvshist` is a `from to weight` text table (saturating-255 visibility history — bake-tool input; no `pvshist` reference exists in the exe). `city/<stem>.lmap` is `LMP0` + one i32 per room (london 1341 = rooms; sf 1125 vs 1171 rooms — authored shortfall; entry 0 is a 0xCDCDCDCD sentinel) and *is* runtime-loaded (`wrong lightmap version`/`room count mismatch` diagnostics in the exe); value semantics unrecovered. `city/<stem>.water` authors a water level plus room refs (london −3.8 @345/351/356; sf −1.9 @228/399/401) — the refs are verified 1-based PSDL room ids resolving to flat attribute-less perimeter rooms at the water plane (the 0-based reading lands sf's 401 on a road tunnel — refuted). Deadly marking is verified from `cityLevel::Load`: refs and an SDL pass (room's first attribute is a `TextureRef` to a drowning-class surface — 42 sf/20 london rooms, all `deepwater`-mapped; `water`-mapped shallow surfaces don't mark) set the same runtime flag (mm2hook `RoomFlags::Water` = 0x4), and the kill test is `flagged && pos.y < level` against the one global level — London's BAI lanes at −6.7…−22 carry no mark. The base `.cpvs` now culls per-room render meshes at runtime (F18-A.5, DSN-33). `.water` + the SDL marks feed the recovery classifier as a room-scoped overlay (F18-A.6/.7, DSN-34 — exposure shape designed, marking verified). | verified_original | `mm2-inspect weather` on retail; angel-file-formats `CPVS.md`; MM2Hook `IsRoomVisible`/`RoomFlags`; Midtown2.exe disassembly; docs/research/environment.md |
| WLD-24 | Each city authors a per-preset fog table `city/<stem>_fog.csv`: a `fog red,fog green,fog blue,fog start,fog end,description (ignored)` header then 16 rows `r,g,b,start,end,label`. The row index is the same `NN = tod×4 + weather` slot as `.ltNN` — every row's label equals the `.ltNN` block name at its position on both cities (16/16, audit-enforced) and the header's own "(ignored)" tag confirms positional reading. The table lands on MM2Hook's recovered `lvlSky::FogColors[16]`/`FogNearClip[16]`/`FogFarClip[16]` indexed by `TimeWeatherType`; `fog start`/`fog end` are the near/far clip distances (metres — linear band inferred from the field names, values authored). Bands differ per city: london `clear-morning` 220–320 m vs sf 650–1000 m; foggy slots are extreme (sf `foggy-morning` 2–100 m). `city/sf_fog_orig.csv` is an audited extra — the same shape with space-labelled rows and wider bands, an earlier authored revision, never consumed. The table now binds to Bevy `DistanceFog` on the session cameras (DSN-30). | verified_original | `mm2-inspect weather` on retail; MM2Hook `lvlSky`; docs/research/environment.md |
| WLD-25 | PSDL sidewalk height convention: road-edge (kerb-foot) chains run at road level and the sidewalk top stands 0.15 m above — "the road surface vertices are expected to be located 0.15 units below the sidewalk vertices" (`Room_attributes` format doc). Measured on retail: `sw.y − road.y == 0.15` on 943/1087 london and 1205/1321 sf `RoadWithSidewalks` sections (residuals are authored flush ramps/driveways) and `top.y − ground.y == 0.15` on all 5 880 london / 5 665 sf `SidewalkStrip` pairs; `SidewalkStrip` end caps mark their apex `a + 0.15`. `RoadNoSidewalks` walkway edges are already at surface height and carry no kerb. Prop-rule stamps therefore evaluate the kerb↔outer lerp on the lifted chains (`mm2_game::SIDEWALK_KERB_LIFT`, shared with `mm2_app::city`'s emit) — an implementation aligned to the authored surface, not a claim about the original's stamping internals. | verified_original | `Room_attributes` doc quote in `mm2_app::city`; measured on `city/{sf,london}.psdl`; docs/research/proprules.md |

## Audio

| ID | Rule | Class | Source |
| --- | --- | --- | --- |
| AUD-1 | All 2 503 shipped `.wav` files are uncompressed RIFF/WAVE PCM (2 112 @ 11 025 Hz mono, 375 @ 22 050 Hz mono, 13 @ 48 000 Hz mono, 3 stereo/other-rate stragglers; 240.9 MB, ≈2.84 h). `aud/aud22/{engines,horns,impacts,sirens,surfaces,suspension,amb3d,creature3d}` holds gameplay samples; `aud/aud11/{al*,as*,cc*}` is speech-dominated. | verified_original | `mm2-inspect audio` on retail; docs/research/audio.md |
| AUD-2 | Wave filenames carry a rate suffix (`*.11k.wav`/`*.22k.wav`); cardata sample references carry neither suffix nor directory — `enginesedan1` → `aud/aud22/engines/enginesedan1.22k.wav`. 159/160 distinct references resolve; `skidflagstone` is a dead authored ref in the dry/wet surface tables. Whether `aud11` variants ever serve these refs is unverified (UNK-25). | verified_original (resolution), inferred (suffix policy) | `mm2-inspect audio` cross-check; docs/research/audio.md |
| AUD-3 | Per-vehicle audio lives in `aud/cardata/{player,opponent}/<id>.csv`: a horn/clutch row then an `Engine wave name` table of RPM-faded loops. Three authored column layouts exist — 10-column fade-window (canonical), 9-column `Pitch divisor`, 7-column `Volume divisor`/`vol inverse RPM` (dev work copies). `Num Engine Samples` drifts from authored rows on ~35 files (players declare 2 author 4; opponents declare up to 4 author 1). Every `tune/vp*.info` id has both cardata files; `vpcaddie59` is the one orphan (no tune entry). | verified_original | `mm2-inspect audio` on retail; docs/research/audio.md |
| AUD-4 | `engineparams{play,opp}.csv` rows open with an 83–97-byte binary name field (MSVC `0xCD`/`0xDD` debug fill) then nine headerless floats — an authored serialization quirk preserved verbatim, column meanings unverified. | verified_original | `mm2-inspect audio` on retail; docs/research/audio.md |
| AUD-5 | `default_impacts.csv` (player/opponent) lists 24 banger-name categories × 28 samples with `min force`/`max force`/`frequency` fields; `dgBangerData.AudioId` is 0 on every retail record so the category↔prop binding is by name at best — mechanism unverified (UNK-25). | verified_original (shape), unknown (binding) | `mm2-inspect audio` on retail |
| AUD-6 | `default_surface{dry,ice,wet}.csv` carry a `Tunnel sound index` (0 on dry and opponent wet, 5 on ice — and on the player wet file) plus positional surface rows. The schema split is per **variant**, not per side (corrected 2026-09-24 — an earlier reading had it per side): dry/wet files on both sides use a `max speed`-window row whose skid bands key on `min slippage,max slippage` (opponent wet adds `for tunnels`); the ice files on both sides use a 12-column `surface vol/pitch divisor` + `skid vol divisor` + `for tunnels` layout whose skid bands key on `min speed,max speed` — a different trigger unit for the same table slot, preserved not normalized (UNK-25). | verified_original | `mm2-inspect audio` + direct `mm2aud.ar` extraction on retail |
| AUD-7 | `*policesiren.csv` programs are `Sample name` + `play time,next index` step chains (headers repeated per step); player files add an `Explosion sample` row. `aud/ambient/*container.csv` are `file names` lists of sibling emitter tables (`VECTORPOINTS` position lists up to 99 pts). `shared/{semidata,vehtypes}.csv`, `suspensionaudio.csv`, `tirewobble.csv` all parse. | verified_original | `mm2-inspect audio` on retail; docs/research/audio.md |
| AUD-8 | `aud/dmusic` is DirectMusic: 95 RIFF containers — `.sgt`=`DMSG`×62, `.sty`=`DMST`×17, `.dls`=`DLS `×14, `.bnd`=`DMBD`×2 — recognized by form word, not decoded (F08). `aud/spchdata` (526) and `aud/creaturedata` (21) text CSVs are deferred cue-table grammars (F08). | verified_original | `mm2-inspect audio` on retail; docs/research/audio.md |
| AUD-9 | `aud/cardata/ambient/` ships 8 `*_engine.csv` tables — `default_engine` plus `va_bus_f`, `va_ddbus_l`, `va_diesels_s`, `va_garbagetruck`, `va_pickup_f`, `va_sedan_s`, `va_smallsuv_s` — each one `Engine sample` + `engine volume` row then `(min speed,max speed → engine min pitch,engine max pitch)` bands in authored order, the tight piecewise bands followed by a `0–500` catch-all with extreme pitch range (up to ~24.6 on `va_sedan_s`). Roster-name coverage: only `va_bus_f`/`va_ddbus_l`/`va_diesels_s`/`va_smallsuv_s` match a rostered ambient id exactly — `va_sedan_s_engine.csv` has no `va_sedan_s` roster id (the roster's is `va_sedans_s`) and `va_garbagetruck`/`va_pickup_f` are unrostered in both stock cities, so under exact-match resolution those three tables are dead data and everything else reads the default. Which classes the original actually binds and how it applies the bands is unverified (UNK-25). | verified_original (shape), unknown (binding) | `mm2-inspect list` + `mm2-inspect audio` on retail; docs/research/audio.md |
| AUD-10 | `*policesiren.csv` programs bind to vehicles through the horn-row `flags` bitmask (corrected 2026-09-24 — an earlier read had `flags` always 0): retail authors `4` on `vpcop` alone, `1` on `vpbus`, `2` on `vpcentury`, `8` on `vpddbus`/`vpsemi` — identical on the player and opponent cardata, every other id 0. The exe names `sfpolicesiren`/`londonpolicesiren`/`policesiren` and `sirens\%s` — city-keyed player programs, one shared opponent program, waves scoped to `aud/*/sirens/` (every program stem also ships there; some additionally ship flat `aud11` copies). `next index` values are program-sample positions, all in range — sf's four samples cycle, london's pair ping-pongs, the opponent's three loop — no authored termination exists; the `Explosion sample` row (player files only) has no recovered consumer. `vpsemi` authors `FIRETRUCKSIREN` as its *horn* under flag 8, suggesting the bits pick the horn control's behavior family — but bits 1/2/8's meanings, the flag-4 trigger semantics and the step-selection rule are unverified (DSN-42, UNK-25). `siren0`/`siren1` exe strings are the cop car's light-bar mesh names, not audio slots. | verified_original (files, flag census, exe strings), unknown (semantics) | `mm2-inspect dump` on retail cardata + `strings Midtown2.exe`; docs/research/audio.md |
| AUD-11 | The retail exe's strings name exactly two runtime surface-table variants — `%s_surfacedry`/`default_surfacedry` and `%s_surfacewet`/`default_surfacewet`, a `%s_`-prefixed (per-name) probe ahead of the `default_` fallback — and contain **no** `surfaceice` string anywhere: the shipped `default_surfaceice.csv` files (AUD-6's divisor schema) are dead authored data under every recovered binding, kept parsed and preserved but with no runtime consumer. What fills `%s` (the vehicle catalog id is the inferred reading — stock ships no per-vehicle surface file to compare) and which session state selects wet vs dry are unrecovered (UNK-25; the runtime's rainy→wet mapping is designed, DSN-43). | verified_original (exe strings, file census), inferred (probe stem), unknown (selection state) | `strings Midtown2.exe`; `mm2-inspect audio` on retail; docs/research/audio.md |

## Deliberate rust-mm2 departures (designed, not original claims)

| ID | Policy | Source |
| --- | --- | --- |
| DSN-1 | Handling assists (steering slip, levelling, gearbox) intentionally deviate from retail numbers; per-field provenance is in the `ConversionReport`. | docs/vehicle-handling.md |
| DSN-2 | Rust/Bevy/Avian stack, wgpu rasterized rendering — a modern engine, not a Direct3D emulation; original renderer options (CTL-4) inform features, not implementation. | PROJECT.md, docs/architecture.md |
| DSN-3 | Modern engine-to-engine multiplayer only; no DirectPlay/Zone/serial/modem compatibility. | PROJECT.md |
| DSN-4 | VFS reads installs read-only with deterministic mod overrides. | PROJECT.md, docs/modding.md |
| DSN-5 | Shared race runtime defaults not pinned by authored data: checkpoint vertical band ±8 m, direction-check flag off, 3 s start countdown at 120 Hz. Provisional until real event evidence exists. | `mm2_game::race` constants + docs |
| DSN-6 | Event start without authored `_strtpnts`: the player spawns on the start line facing the row0→row1 tangent — the line is the one point the authored data guarantees is on the course (a 10 m tangent back-off spawned london `blitz6` past the edge of its elevated start deck, over a void; changed 2026-09-20). Checkpoint markers are translucent orange columns, finish a green column — dev-rig visuals, not the original gate rendering. | `mm2_content::race_def`, `mm2_app::race` |
| DSN-7 | Runtime deadline semantics: `TimeLimit` binds as seconds → 120 Hz ticks on Blitz rows only (provisional, UNK-4); the constant values on Checkpoint/Circuit rows stay unbound. The deadline is inclusive — a finish crossing on the expiry tick counts because segments evaluate before the timeout check; expiry records one authoritative `TimedOut` result per unresolved participant. Remaining time displays `m:ss` from the same race clock. | `mm2_content::race_def`, `mm2_game::race`, `mm2_app::race` |
| DSN-8 | Navigation-arrow presentation (RACE-6): a UI needle + diamond tip stands in for the original bitmap arrow (the embedded font is ASCII-only). It is hidden under `Ordered` rules per HUD-2's instrument list, and aims at the armed finish once every gate clears (inferred — RACE-6 names only checkpoints). Cycle keys are X/Z, not the original X/S (CTL-1): `S` is brake under this app's added WASD mapping — an input-map departure, not a rules one. | `mm2_game::race` nav functions, `mm2_app::race` arrow |
| DSN-9 | Low-time warning cue: no documented original rule describes a Blitz low-time warning (HUD-2 lists only the countdown timer among race instruments), so the HUD pulses a `LOW TIME` banner once `time_remaining` reaches 10 s at the race tick rate, alternating bright/dim every 0.5 s of remaining time — derived from the same race ticks that judge the deadline, so it freezes with a pause and clears on resolution. Presentation policy, not an original-behavior claim; an audio cue stays impossible until F07 exists. | `mm2_app::race` `LOW_TIME_TICKS`/`update_race_warning` |
| DSN-10 | Banger runtime slice (F04-A.3 + F04-B.1): bound pathset stamps become one session-owned entity (collider + dormant `Banger` + `ObjectIdentity`/`AuthorityRole`, mesh parts as children) instead of the loose render/collider pair. `activate_bangers` compares the striker's kinetic energy `½·m·v²` (deepest manifold contact's approach speed, shared with the impact pipeline) against `ImpulseLimit2` — a provisional stand-in for the unknown original quantity (UNK-22), chosen because the authored limit ladder is quadratic in speed (F04-C.5 census, docs/research/banger.md). A qualifying edge flips `RigidBody` to dynamic once, replays the contact as a reduced-mass two-body transfer (the solver's static wall impulse is returned and `(1+e)·v·μ` paid instead — F04-C.4, implementation choice under UNK-22) plus a `Size`-derived spin kick, and emits `BangerStateChanged`; a ×32 `BangerPool` (the R4-recovered pool size) reclaims oldest-first; a slept body settles back to `RigidBody::Static` (`dgHitBangerInstance`-like, terminal for the session — the recovered `Timer` despawn is unimplemented). A placement carrying collidable `BREAK<NN>` chunks instead goes `Broken` at the same activation edge (provisional timing, UNK-22): parent collider + mesh children are removed and each piece spawns as its own `Active` fragment body (own `ObjectId`, own `<name>_break<NN>` record or the parent def, convex collider, render children) claiming pool slots — one `BangerStateChanged` per logical break. Predicted sessions never transition banger state. `BirthRule`, audio/flash/decal effects and replication stay deferred; prop-rule placements now stamp through the same dormant-banger path as pathset stamps (WLD-17/UNK-21). | `mm2_game::banger`, `mm2_app::banger`, `docs/research/banger.md` |
| DSN-11 | Results flow (UI-5, documented): the *local* participant's terminal resolution ends the playing session — `advance_race` transitions `Playing → Results` on the same step it records the `Finished`/`TimedOut` result, so the race clock and ledger freeze with the phase. A remote/AI participant resolving while the local driver still races changes nothing (AC03). `RacePhase::Complete` still waits for every participant to resolve; the Results phase surfaces outcome + place + recorded finish time on the HUD line (place per DSN-12) and, since F17-B.2, a session-owned results overlay presents the local outcome, the generation-scoped standings (unresolved participants listed as still racing, never placed), granted rewards, the persistence disposition from `SessionReport`, and Continue/Restart rows. | `mm2_app::race::advance_race`, `mm2_app::main::update_hud`, `mm2_app::results` |
| DSN-12 | Standings ordering (`ResultLedger::standings`): `Finished` outranks `TimedOut` regardless of times, `Finished` orders by the recorded `race_ticks`, and equal ticks order by `PlayerId` — an explicit, deterministic tie-break independent of recording/query order (no verified original placing rule exists; F14 spec demands an explicit tie policy). A participant with no recorded result is unplaced, not ranked last; DNF `TimedOut` ranks below every finish. The HUD shows `FINISHED {ord}[ of {n}]` from this ordering and the smoke record gains `place=`. | `mm2_game::result`, `mm2_app::main::update_hud`, `mm2_app::smoke` |
| DSN-13 | Live running order (`live_order`) — the place indicator's ordering while a race runs (HUD-2 names the instrument; no verified original rule describes it): `Finished` participants lead ordered by their recorded `race_ticks` (same key as DSN-12's standings, so the live order converges to it as everyone resolves), then active participants by progress — `Ordered` counts `(lap, next)`, `AnyOrder` counts cleared gates — progress ties break toward whoever is closer (straight-line XZ) to their *own* current objective (`checkpoints[next]` / `navigation_target`'s nearest remaining gate or armed finish), `TimedOut` trails by `race_ticks`, and every remaining tie orders by `PlayerId`. Straight-line distance is a heuristic, not course distance; results never consume this order — the ledger owns them. The HUD shows `{ord} of {n}` only with ≥2 participants (a place indicator is a competitive instrument); the smoke record reports `pos=` whenever the local participant has a standing. | `mm2_game::race::live_order`, `mm2_app::main::update_hud`, `mm2_app::smoke` |
| DSN-14 | Opponent bounded re-anchor (F15-B.3): an opponent that spends 900 driving frames (~15 s) without 8 m of displacement — penned, hull-beached with unloaded wheels (the scripted stuck detector only counts grounded cars), or knocked off its route — teleports onto the route leg it was chasing through the production `ResetVehicle` path, walked backward along the authored polyline (4 m clearance, further while inside an un-cleared trigger, ≤60 m total). The jump is *disclosed*: `Teleported` breaks the swept segment so it cannot sweep checkpoints, the walk-back keeps the landing out of pending gates, `OpponentDriver::reanchors` counts each assist, and the smoke record surfaces the field total as `opp_rec=`. Authority role only — a predicted client never teleports a participant. No verified original recovery rule exists (retail opponents plausibly despawn/reset; unmeasured), so this is a designed anti-standstill policy, not an original-behavior claim. | `mm2_app::opponents` (`REANCHOR_*`, `reanchor_pose`, `opponent_drive`), `mm2_app::smoke` |
| DSN-15 | Profile store (F16-A.1): our own versioned JSON save format — no compatibility with the retail `players/*.sav`/`*.cfg` binaries is claimed (F16 non-goal). One `<id>.json` per profile under an OS user-data dir (`~/Library/Application Support/rust-mm2/profiles` macOS, `%APPDATA%\rust-mm2\profiles` Windows, `$XDG_DATA_HOME`/`~/.local/share` other Unix); original installs stay read-only. `driver-<n>` ids allocate from a persisted `next-id` high-water mark (advanced before the new profile's first save, floored at the highest surviving file suffix) so a deleted id is never reused; display names may repeat (the id is the identity). Saves are atomic — tmp write + `sync_all`, rotate `.bak`, rename + directory fsync — and `load` keeps the highest surviving `revision` across main/`.tmp`/`.bak`, reporting `recovered_from_backup`; corrupt files still `list()` by id and are never deleted by a read. `version` is stamped on every write and any mismatch rejects the file rather than guessing; unknown top-level fields round-trip through a preserved `extra` map. Progress keys are `EventKey{city, table, stem}` — the authored file stem, not the table row index — so a mod inserting a row cannot retarget a saved record. `ProfileKind::Sandbox` gates `records_progress()`, the single check the F16-B reward path must make. DRV-7 is enforced at the store: the last remaining profile cannot be deleted. | `mm2_game::profile` |
| DSN-16 | Progression application (F16-B.1): `race/<city>/<city>_rewards.csv` normalizes into a session `RewardTable` — `{blitz,circuit,race},half|all` milestone rows (`race` = Checkpoint, the authored token) measured against the catalog's authored family sizes, and indexed `<family>,N` rows bound to the event whose stem is `<prefix><N>` (`crash,3` → `crash3`; the binding is family-aware — an indexed `race,N` row would attach to `race<N>`). `VariantNum` 0 unlocks `vehicle:<id>`, nonzero `paint:<id>:<variant>` where the variant is the zero-based paint index — measured on retail (VEH-4): vpvwcup's variant 5/6 land on "Team Angel"/"Team MS", the documented Angel/Microsoft Cup paints, and every other authored variant lands on its car's declared `Colors` index. An event counts as *beaten* when a `Finished` result meets the documented place criterion (top-3 Amateur, 1st Professional — RACE-3/CHK-3/VEH-3/VEH-4); a solo event finishes at place 1, so both ranks reduce to "finish". `EventRecord` persists `finishes`, `best_race_ticks`, `best_place`, and per-rank `beaten` flags — milestones and indexed rewards read the flags, so repeat finishes and re-delivered results never re-grant (the `unlocks` set is idempotent, F16-AC02) and a record a hand-edited file lost re-grants on the next beaten finish. Results apply only through `record_session_results` draining the authoritative `ResultLedger` — never UI navigation (spec req 3) — into a bound standard profile; sandbox identities (`records_progress()` false) skip it entirely. `record_eligibility` extends DRV-6's default-conditions rule: dev-world sessions, the synthetic dev car, gameplay-affecting `DevOverrides` (`vehicle-config`, `spawn`, `banger-pool`, `traction` ≠ 1.0, `finish` — the presentation-only `--cam`/`--nav`/`--pause` are not counted) and any mounted mod (`SessionConfig.mods_active`, a conservative stand-in until F29 classifies per-mod impact) make every result ineligible, and a `--bot`/`ScriptedDrive` finish never banks progress — the scripted driver is an evidence tool, not the player. `TimedOut` results record nothing. Event *availability* (CHK-2/CHK-3's set-of-three gating, CC-3's lesson→midterm→final chain, RACE-3's per-race customization) is derived state over the same `beaten` flags — DSN-17's `AvailabilityTable`; the menu that enforces it is F17. `vpmoonrover` has no reward row (UNK-3 stands) and Pro points (DRV-4/UNK-8) are still unimplemented. Retail audit (`mm2-inspect events`, 2026-09-21): each city normalizes to 4 event-bound + 6 milestone rules over Blitz=10 Checkpoint=12 Circuit=10 CrashCourse=13 authored rows with zero unresolvable rows. | `mm2_game::progression`, `mm2_content::rewards`, `mm2_app::progression` |
| DSN-17 | Event availability (F16-B.2): `AvailabilityTable` is derived state over the persisted `beaten` flags — one row per authored catalog event, evaluated per query, never stored. Gates: Checkpoint rows group into authored-order sets of three — set 0 open (CHK-2), each later set `AfterAll` the previous set (CHK-3); Crash Course rows read the authored `Description` tag (`lesson<N>`/`midtrm<N>`/`final…`, CC-2) — lessons open, `midtrm<N>` gates `lesson{3N-2}…{3N}` (the tag arithmetic matches the authored order; the tag→group binding is a designed reading of authored labels), `final` gates every midterm (CC-3); Blitz/Circuit rows carry no authored gating. RACE-3's per-event customization is the same `is_beaten` flag on the event's own record (CC-4's passed-lesson any-vehicle replay rides it for the crash family). Designed edges, all fail-open + diagnosed: an unreadable crash tag, a midterm with no lesson group, a final with no midterms stay available and land in `diagnostics`. A sandbox profile evaluates to the unrestricted view (spec req 5's dev access). Until F17's menu enforces selection, a `--event` launch of a locked event runs but warns with the un-beaten prerequisites — the CLI is a dev act, not selection. Retail audit (`mm2-inspect events`, install `fnv1a64:e91e6cd4b2ae30d9`): both cities 32 open / 13 gated / 0 diagnostics — `race3-5 ← race0-2`, `race6-8 ← race3-5`, `race9-11 ← race6-8`; `crash3 ← crash0-2`, `crash7 ← crash4-6`, `crash11 ← crash8-10`, `crash12 ← crash3,crash7,crash11`. | `mm2_game::progression` (`AvailabilityTable`), `mm2_content::availability`, `mm2_app::progression::EventRewards` |
| DSN-18 | Vehicle/paint selectability (F16-B.3): `GarageTable` is derived state over the persisted `unlocks` set — one row per catalog entry, evaluated per query, never stored. Gates come from the authored reward tables, not `.info` `UnlockScore`/`UnlockFlags` (VEH-5 shows those don't correlate — kept verbatim on the row for UNK-6 audit only). A `vehicle:<id>` grant (authored `VariantNum` 0) gates the car; a `paint:<id>:<variant>` grant gates that paint index — variant = the zero-based `Colors` index, measured (DSN-16). A locked vehicle reports every paint locked; unlocking the car does not open its gated paints (each needs its own grant). Every city's reward table feeds the union — a London unlock opens in SF's garage too. Roster membership is the canonical `tune/<id>.info` scan (the original's vehicle list; designed reading): fallback-extension (`.inf`/`.vinfo`/`.info.bak`) or metadata-less entries — vpmoonrover's `.inf` is the retail case, UNK-3 — are `listed=false` but still evaluate. Grants for uncatalogued ids or out-of-range variants land in `diagnostics`, never dropped (F16-AC05's accounting). A sandbox profile evaluates to the unrestricted view. Until F17's garage enforces it, a locked/unlisted `--car` or remembered selection launches but warns (same interim as the locked `--event` warn). | `mm2_game::progression` (`GarageTable`), `mm2_content::garage`, `mm2_app::profile::vehicle_gate_note` |
| DSN-19 | Start-countdown presentation (F17-B.3): a session-owned centered banner shows `ceil(remaining / RACE_TICK_HZ)` — one second per digit — while `RacePhase::Countdown` runs, then `GO!` for `COUNTDOWN_GO_TICKS` (1 s) of the race clock after release. No documented original rule pins the cue's look (DSN-5's 3 s countdown is itself a provisional default; HUD-2 lists no start-cue instrument), so digits/GO/position/duration are a presentation policy, not an original-behavior claim. Deriving both halves off the race state keeps them deterministic: the digits track the countdown's own `remaining` ticks and `GO!` is a `Playing`-phase flash measured in race-clock ticks, so it cannot linger under the pause or results overlays even when the frozen clock still sits inside the window. A zero-length countdown shows only `GO!`. | `mm2_app::race` (`CountdownBanner`, `update_countdown_banner`, `COUNTDOWN_GO_TICKS`) |
| DSN-20 | Stuck detection and recovery (F05-B.2): the authored `vehstuck` bounds drive a designed interpretation of MM2Hook's recovered `vehStuck` struct (its `Update()` body is unrecovered, so the test combination is ours, UNK-13): an `ImpactEvent` anchors the detector at the car's pose (`m_LastImpactPos`), `pos_thresh`/`move_thresh` form a hysteresis band around it (uniform `move > pos` on retail), rotation past `turn` re-anchors the settle window as still-tumbling, and `time_thresh` accrued inside `pos_thresh` fires one bounded `StuckEvent`. Recovery is an in-place upright `ResetVehicle` (heading kept, hull dropped onto the resting surface — `Rotation` 0 and `Translation` ≈ 0.1 on every retail record author no positional rescue or yaw change), trailers re-seated at authored offsets; the reset marks `Teleported` so it cannot sweep a checkpoint. `Rotation`/`Translation` decode verbatim but are not consumed. Remote participants' detectors belong to their authority; `Disabled` wrecks belong to the damage outcome. The authored `TimeThresh` also supplies `assists.self_right_delay` for the pre-existing modern self-righting assist, which still covers impact-free rollovers the authored detector never arms on. | `mm2_game::stuck` (`VehicleStuck`, `StuckEvent`), `mm2_app::stuck`, `docs/research/damage.md` |
| DSN-21 | Breakaway detachment (F05-B.3): each `dgbangerdata`-backed `BREAK<NN>` part detaches on the first impact whose `severity × part_mass` — the impact's approach speed measured against the part's own record `Mass`, a different quantity from the `severity × other_mass` estimate `apply_impact_damage` weighs — exceeds that part's authored `ImpulseLimit2`, so `limit / mass` reads directly as the authored detach speed (measured ≈31.25 m/s on ordinary panels, ≈2500 m/s on the heavy rigs' anchors — the reading the authored values are shaped for; what the original compares the limit against stays UNK-22). Whether the original detaches on this quantity, on accumulated damage, or on a different comparison is unrecovered (UNK-13). On detach the intact part node hides and a fragment body spawns at the detached mesh's pose: convex hull over the part's own baked verts, the record's physicals (mass/friction/elasticity), the car's velocity at the part centroid plus the impact kick and spin, pooled through the shared `BangerPool` like prop debris. One bounded `PartDetached` fires per part per attachment; a part leaves the rig even when the pool bound denies a body (`fragment: None` on the event). The record's `CG`/`Size` conventions on vehicle fragments mix car-space anchors with ~zero conventions (not reconciled — UNK-13), so the fragment's centre of mass is the hull's measured centroid, not the record field. Repair — the disabled outcome's reset, wherever it lands — re-attaches every part and despawns its fragments; a plain reset or stuck recovery is not a repair and leaves parts off. Parts whose record is missing or malformed never enter the rig (same authored-absence rule as damage/stuck); remote participants' rigs belong to their authority (F25+). | `mm2_game::breakaway` (`VehicleBreaks`, `PartDetached`), `mm2_app::breakaway`, `docs/research/damage.md` |
| DSN-22 | Gyro maneuvers (F05-B.4): `VehicleConfig.gyro` carries the decoded `vehgyro` record verbatim (`None` on authored absence, never fabricated; malformed negative/non-finite rates warn and clamp to 0 at conversion). The application is a designed reading of the recovered `Spinable`/`Driftable`/`Rightable` gates — the original's `Update()` is a thunk, so trigger tests and mechanism stay UNK-13. Handbrake + steering while travelling latches `VehicleState::gyro_spin` — `Spin180` forward, `Reverse180` in reverse (the J-turn the Crash Course teaches) — and the gyro then *is* the yaw authority, writing the authored rate each step rather than applying a torque the tires' kinetic friction could stall once the car scrubs its speed (the recovered `mmCarSim` keeps a `SpinState` machine for the same reason a per-frame gate cannot express the maneuver: forward speed collapses to zero at 90°). The hold doses the rotation — release ends it partway, ~π counts a completion, an opposite flick re-arms, a bounded age prevents indefinite servoing. `Drift` relieves the slip term of the yaw damper so a drift-authored car holds a controlled slide; `drift = 0` is the unmodified policy exactly. `Pitch`/`Roll` are per-axis airborne righting rates shaped on the `air_control` assist — authored 0.0 or absent on every retail record, so inert on stock content. `gyr=` smoke evidence appears only on maneuver activity; the scripted driver never handbrakes, so stock cruises emit no `gyr=` — though the live `Drift` relief does subtly alter slide dynamics on drift-authored cars (vpbug's SF-cruise endpoint moves ~1 m, counters identical). | `mm2_vehicle::config` (`GyroConfig`), `mm2_vehicle::systems` (spin latch, drift relief, righting), `docs/research/damage.md` |

| DSN-23 | Water/out-of-bounds recovery (F05-B.5): no authored record exists for water or out-of-bounds rescue — the damage-family census (DMG-5..8) covers `vehcardamage`/`vehstuck`/`vehgyro` only and DMG-2 covers destruction, so every bound is designed (the original's rules stay UNK-13). `VehicleRecovery` rides on every local/AI participant (a predicted client's authority owns its own, F05 req 6), pre-anchored at spawn. Any grounded wheel under `RecoveryPolicy::water_min_drag` (0.3 — parts retail `deepwater` 0.5 from shallow `water` 0.119, which stays wadable under the F06-B.2 policy) re-anchors the last recoverable pose; every grounded wheel at/above it accrues `submerge_dwell` (2.0 s — the escape window: a car that powers back onto a dry edge keeps driving) and fires a `RecoveryCause::Submerged` recovery to the anchor — back on shore, not in place on water. An airborne fall `fall_margin` (50 m, sized past retail drops — a real landing refreshes the anchor first) below the anchor fires `RecoveryCause::OutOfBounds`, latched once per fall; a non-finite pose fires at once (defence-in-depth — a pose gone NaN inside the physics step trips the wheel raycast first). `resolve_recovery` answers with `ResetVehicle` (`Teleported`, so no checkpoint sweep), trailers re-seat at authored offsets, an armed stuck episode disarms, and recovery is *not* a repair — damage and detached parts persist. Remote participants skip; `Disabled` wrecks belong to the damage outcome. `rcv=` smoke field on activity only. | `mm2_game::recovery` (`VehicleRecovery`, `RecoveryEvent`), `mm2_app::recovery`, `docs/research/damage.md` |
| DSN-24 | Engine smoke visual tier (F05-B.6): the `vehCarDamage` embedded particle spec (mm2hook's recovered `EngineSmokeRule`) renders as billboard puffs. Authored: both pivots (`SmokeOffset`, non-zero `SmokeOffset2`), the `DoublePivot` gate (emit-all vs `m_CurrentPivot` alternation), every consumed particle field (`PositionVar`/`Velocity`/`Life`/`Radius`/`Drag`/`DRadius`/`DAlpha`/`Gravity`/`TexFrame` range/`Color`), and the `fxpt` atlas-tile vocabulary (`fxpt2` is a measured 2×2 puff atlas; frames index tiles like `asSparkPos::TexCoordOffset`). Designed (`vehCarDamage::Update()` is a binary thunk, UNK-13): the MedDamage→MaxDamage emission ramp (4→24 puffs/s), the mirror-about-x reading of a non-zero `MirrorPivot` (all retail values are 0), the per-field integrator readings (gravity as +Y rise, exponential drag, byte-space `DAlpha` fade), the `fxpt2` texture binding, and the 48-puff live bound. Deterministic per-vehicle `NavRng` seeding (object id) keeps the emission stream replicable; remote participants skip like every F05 system, pause freezes, puffs are session-stamped and expire on authored `Life`. `TextelDamageRadius`/`ImpactsTable` texel damage and `asLineSparks` are covered by their own legs (DSN-32, DSN-26). `ptx=` smoke field on activity only. | `mm2_game::effects` (`ParticleSpec`, `VehicleSmoke`, `SmokePuff`), `mm2_app::damage_fx`, `docs/research/damage.md` |
| DSN-25 | Engine impairment (F05-B.7): MM2Hook's `mm2.ini` `PhysicalEngineDamage` option documents the original coupling — "when the engine spews smoke" the vehicle has "less acceleration and less top speed" — but its magnitude, ramp and onset are unrecovered (`vehCarDamage::Update()` is a thunk, UNK-13), so the implemented shape is designed. `ImpairmentPolicy::factor` keys on the same authored `MedDamage` bound the DSN-24 smoke gate emits on (the documented coupling holds by construction): 1.0 below it, `power_at_med` 0.8 on reaching it, a linear ramp to `power_at_max` 0.4 at `MaxDamage`; degenerate/non-finite inputs degrade to full output. `mm2_vehicle::EngineImpairment` is the physics-side input — it scales only `wheel_drive_available` (forward and reverse drive torque); brakes, `engine_brake_nm`, steering and tire forces are not engine output and stay unscaled; the sim sanitises the factor (non-finite → 1.0, clamps 0..1). `mm2_app::damage::sync_impairment` mirrors each local/AI participant's authored total into the component — present exactly while `factor < 1`, cleared the same tick a repair returns the state to `Intact`; remote participants are skipped (F25+), pause freezes. `imp=` smoke field on activity only — undamaged runs carry no component and stay bit-identical. | `mm2_game::damage` (`ImpairmentPolicy`), `mm2_vehicle::vehicle` (`EngineImpairment`), `mm2_app::damage::sync_impairment`, `docs/research/damage.md` |
| DSN-26 | Impact sparks (F05-B.8): mm2hook's recovered `vehCarDamage` owns a per-vehicle `asLineSparks` renderer fired from the car's `ImpactCB` — `RadialBlast(count, position, velocity)` is the emission primitive, a radial burst at the impact point. Authored/recovered: the per-vehicle renderer ownership (rigged exactly when `vehcardamage` decodes, same gate as `VehicleSmoke`), the impact feed (the deduplicated `ImpactEvent` stream's authored contact point/normal), and `texture/spark.tga` (the install's only spark-named texture — an 8×8 fleck; the original's `Init` texture name is unrecovered). Designed (`vehCarDamage::Update()` is a thunk and `SparkMultiplier` is runtime state no retail tune record authors, UNK-13): burst sizing (`min_burst` 2 + `sparks_per_speed` 0.8/m/s, ceiling `max_burst` 16, per-vehicle `max_live` 128), the rebound spray (`rebound_dir` + ±`spread` 0.8 lateral jitter renormalized at `speed` 7 ±3 m/s off the participant's side of the contact normal), `life` 0.5 ±0.2 s, `gravity` 9.8 m/s², the velocity-aligned crossed-quad streak render (width 0.03 m, speed-scaled length floored at 0.04 m) and the additive-blend linear fade. A missing `spark.tga` warns and falls back to an untextured additive material (emission continues); an absent `SparkFx` resource emits nothing. Per-vehicle `NavRng` seeding (object id) keeps bursts replicable; remote participants skip like every F05 system, a non-`Playing` frame drains the feed without emitting, streaks are session-stamped and despawn on `life`. `spk=<b>b/<e>e/<x>x` smoke field on activity only — impact-free runs stay bit-identical. `TextelDamageRadius`/`ImpactsTable` texel damage is covered by its own leg (DSN-32). | `mm2_game::effects` (`SparkPolicy`, `VehicleSparks`, `Spark`), `mm2_app::spark_fx`, `docs/research/damage.md` |
| DSN-27 | Opponent catch-up assist (F15-B.4): whether the original rubber-bands trailing opponents at all is unverified (UNK-11), so the whole policy is designed — the F15 spec permits rubber-banding only when it is "explicit, observable, scoped by rules and not falsely recorded as physical racing". `course_progress` measures a participant's continuous course position in gate units — banked gates (`lap × gates + next` under `Ordered`, the cleared count under `AnyOrder`) plus the covered fraction of the leg toward the same objective `live_order` tie-breaks on (`checkpoints[next]` / `navigation_target`'s nearest remaining gate or armed finish), scaled by the definition's mean authored gate spacing (`mean_gate_spacing`; an 80 m designed fallback, `CATCH_UP_LEG_REF`, when a course cannot supply a leg). `opponent_drive` takes the leader as the best course position across *every* progress-carrying participant — the human included — and `catch_up_factor` lifts a trailing AI driver's demand ceiling one-directionally: `throttle_cap + assist` (bounded 1.0) and `corner_speed × (1 + assist)`, ramping linearly to `CatchUpPolicy::assist_max` 0.25 at `deficit_full` 2.0 gates behind. Nobody is slowed below authored tuning, the local player is never a recipient, progress is still earned through the same swept triggers, and the lift is observable: `OpponentDriver::catch_up` carries the live factor and the smoke record counts assisted drivers as `cu=` on activity only, distinct from the `opp_rec=` re-anchor count — a lifted demand is not a recovery. Every constant is an implementation choice; a `cu=` field evidences the designed policy firing, not retail fidelity. | `mm2_game::race` (`CatchUpPolicy`, `course_progress`, `catch_up_factor`, `mean_gate_spacing`), `mm2_app::opponents` (`CATCH_UP_LEG_REF`), `mm2_app::smoke` |
| DSN-28 | Environment lighting binding (F18-A.2): the measured `.ltNN` presets (WLD-21) now bind to Bevy through the production session path — `mm2_app::environment::spawn_environment`, called after event resolution so authored `EventParams::conditions` take precedence via the shared `mm2_game::effective_conditions` resolver (RACE-2); `SessionConfig::conditions` is the cruise/dev fallback, selected by `--weather`/`--time-of-day` (session-legal 0-3 grid, out-of-range exits 2). Original/recovered: the `tod*4 + weather` slot map, the per-light directions via R4's `setLightDirectionInv` convention (`to_light_dir`/`travel_dir`), the authored RGBs and the BGRA-packed ambient — all bound verbatim, including `rainy-night`'s below-horizon key. Designed: a uniform 15 000 lux per directional (authored colour carries relative weight, like the original's per-channel diffuse), key-only shadows (fills as bounce), fixed `GlobalAmbientLight` brightness 2 000 anchored near the prior rig for a typical day ambient, and the pre-preset fixed rig as the reported `fallback` on missing/unparseable presets (F18-AC06). Consequence recorded in `environment.md`: authored night ambients out-bright day ones, so night presets render lighter than a real night inside the fog band — authored data, not a selection bug (fog itself bound in F18-A.3, DSN-30). `env=ltNN(name|fallback)` smoke field; `EnvironmentReport` + light entities are session-scoped. `.sky` dome, `.cpvs` PVS, `.ldef`/`.pvshist`/`.lmap`/`.water`, precipitation/wetness/audio and condition replication stay unconsumed (UNK-24). | `mm2_formats::lighting` (`LightSpec::to_light_dir`/`travel_dir`), `mm2_game::race` (`effective_conditions`), `mm2_app::environment`, `docs/research/environment.md` |
| DSN-29 | Menu condition customization (F17-A.6): `Screen::Customize` is the UI-2/RACE-3/RACE-4 options surface — weather, time-of-day and traffic density, the authored option fields with runtime consumers today. Cruise city rows carry an always-open `options` row (RACE-4); every event row is followed by an `options` row gated on `EventAvailability::customizable` — the same `is_beaten` flag DSN-17 derives (RACE-3) — disabled with its reason (no bound driver / unbeaten event / launch-gated event) rather than hidden. The screen seeds its picks from the selected difficulty's authored `RaceParams` (cruise: neutral defaults); an authored selector outside 0-3 or density outside 0..=1 disables the row — the same values `event_params` would reject — instead of fabricating a seed. `SessionConfig::customization` carries the picks: `effective_conditions` prefers them over the authored `EventParams` (lighting binds the picked `.ltNN` slot), and the ambient-traffic density chain takes `customization.densities.traffic` first. DRV-6's default-conditions rule maps onto the field's *presence*: the menu only sets it when the picks differ from the seed, so an unchanged visit launches a record-eligible default run while any changed pick makes the session `Ineligible::Customized` — an implementation mapping of the documented exclusion, not a claim the original stores it this way. The original's option list covers more than today's consumers: pedestrian/cop density (F19/F20 have no consumers) and Circuit laps/opponents stay deferred, and the density picker steps in quarters — an enhanced choice, the authored dial is continuous. | `mm2_game::config` (`SessionCustomization`), `mm2_game::race` (`effective_conditions`), `mm2_game::progression` (`Ineligible::Customized`), `mm2_app::menu` (`Screen::Customize`), `docs/research/menu.md` |
| DSN-30 | Environment fog binding (F18-A.3): the measured `_fog.csv` tables (WLD-24) bind through the same `spawn_environment` call and the same effective slot as the lighting preset — authored event and customization precedence (DSN-29) apply to fog identically. Authored: row RGB → `Color::srgb`, `fog start`/`fog end` → the band distances. Implementation choices: `FogFalloff::Linear{start,end}` on both session cameras (chase + free — the original's fixed-function linear fog per the recovered `FogNearClip`/`FogFarClip` names; the exact curve is inferred, not recovered), directional fog glow disabled (the tables carry no directional data), colour channels clamped 0-255 at bind. Failure policy (implementation): a missing/unparseable table, a slot with no row, or a row whose band cannot interpolate binds *no* `DistanceFog` and sets `EnvironmentReport.fog.absent` (`missing`/`unparseable`/`no row for slot`/`degenerate`) — never a fabricated default; table anomalies count into `fog.issues` without blocking a valid row. The fog channel is independent of the lighting fallback — a fallen-back preset still binds its fog row. `env=` smoke field gains ` fog=<start>-<end>`/` fog=none`. The sky dome is deliberately fog-exempt (DSN-31), so foggy presets still show authored-fog geometry against an unfogged authored sky — the designed reading, not a recovered rule. | `mm2_formats::fog` (`FogTable`/`FogRow`/`FogIssue`), `mm2_app::environment` (`FogReport`/`FogSpec`), `mm2_app::session` (camera `DistanceFog` attach), `docs/research/environment.md` |
| DSN-31 | Environment sky-dome binding (F18-A.4): the `.sky` record (WLD-22) now binds through `mm2_app::environment::spawn_sky_dome` at the same effective slot as the lighting preset and fog row — authored event and customization precedence (DSN-29) apply identically. Authored/measured: the model name → `geometry/<model>.pkg` resolution, and the paint-job grid — measured on retail that `sky_dome_l`'s job *i* textures run `skylondon_{c,p,f,r}{a,n,d,m}_l` in `tod*4 + weather` slot order (`sky_dome` the `sky_*_f` equivalents), so the dome's paint job is the session's preset slot (`slot % jobs` when a dome authors fewer than 16 jobs — designed fallback). Designed (not recovered, UNK-24): `HatYOffset` read as the dome's world-space centre height, `YMultiplier` as its vertical squash, `RotationRate` as radians/second driven by `drive_sky_dome`, the dome scaled from its measured ~43 m extent to a 900 m world radius (inside the 1 000 m far plane, past every authored fog end), re-centring on the active camera in XZ each frame (the horizon policy), unlit + `fog_enabled = false` + `cull_mode = None` materials (the authored texture is the sky's final colour) and `NotShadowCaster`. Failure policy (implementation): missing/unparseable `.sky`, unresolvable/unparseable model, non-finite transform field or empty mesh → no dome + `EnvironmentReport.sky.absent` (`missing`/`unparseable`/`degenerate`/`model unavailable`/`model unparseable`/`empty`) — never a fabricated dome; a missing dome *texture* warns and draws the shared fallback material (prop policy). The dome is render-only: no collider, `SessionEntity`-stamped root + mesh children, session teardown removes it. `env=` smoke field gains ` sky=<model>:<texture>`/` sky=none`; `mm2_app::city::pkg_to_parts` gained a `paint` parameter (prop callers stay on 0) behind the crate-visible `pkg_paint_parts`. | `mm2_app::environment` (`SkyReport`/`SkyDome`/`spawn_sky_dome`/`drive_sky_dome`), `mm2_app::city` (`pkg_paint_parts`), `docs/research/environment.md` |
| DSN-32 | Texel damage (F05-B.9): the `fxTexelDamage` leg of `vehCarDamage`, built on F02-C.4's `_dmg`↔clean pairing (DMG-9). Recovered and implemented: `ApplyDamage(position, maxDist)` walks the body's damage-capable triangles — every tri with a vertex within the authored `TextelDamageRadius` (car space) of the impact point earns one splat at a random barycentric point's UV (three `frand()` draws normalized by their sum), the loop skipping slots with no paired damage texture — and `Reset` re-blits the clean texture over the car's clone at repair. Per-vehicle state mirrors `CurrentShaders`/`DamageTextures`: each paired slot clones the clean image into a writable `current` texture and a cloned material binds it, so one car's skin damage never bleeds into a fleet-mate's. Designed (`ApplyBirdPoopDamage` is an unrecovered binary call, UNK-13): the splat shape — `texel::splat_blit` stamps a probability-dithered 48 px disc of the `_dmg` texture onto `current` at the picked UV (`1 − d/r` + 0.1 edge floor, following mm2hook's debug reimplementation), repeated down the shared mip chain and clipped to bounds. Feed: the same deduplicated `ImpactEvent` stream as `apply_impact_damage`, same `is_playing`/authority/`Remote` gates, the world point mapped into car space through the vehicle transform — every near-body impact stamps, including hits below `apply_impact_damage`'s threshold (the retail `ImpactCB` feeds the table regardless of damage gating). The rig builds only for authored `vehcardamage` carriers with paired body slots — player and AI opponents; ambient traffic and trailers get none; repair runs `TexelRepair::reset` at `resolve_disabled`'s three `damage.reset()` sites while plain reset/stuck/recovery leave the skin stamped. Per-vehicle `NavRng` seeding (object id) keeps splats replicable. `txl=<impacts>i/<splats>s/<resets>r` smoke field on activity only — impact-free runs stay bit-identical. | `mm2_game::texel` (`TexelDamageMesh`/`TexelDamageTri`/`TexelSplat`/`TexelDamagePolicy`/`splat_blit`), `mm2_app::texel_fx` (`TexelSlot`/`TexelDamageRig`/`TexelDamageBuilder`/`TexelRepair`/`TexelDamageReport`), `mm2_app::city` (`MaterialCache::texel_binding`), `docs/research/damage.md` |
| DSN-33 | Room-PVS render culling (F18-A.5): the verified `.cpvs` table (WLD-23) now gates per-room render meshes through `mm2_app::pvs`. Original/recovered: `cityLevel` caches the source room's decompressed list and `DrawRooms` applies `IsRoomVisible` (`code != 0`) per room draw group, `sdlPage16::PointInPerimeter` is 2-D XZ perimeter containment, and `sm_EnablePVS` is the global toggle — `--no-pvs` mirrors it, default on, plumbed through `SessionConfig::dev.no_pvs` so the headless smoke sees it too. Designed (`FindRoomId`'s body is a thunk — only its `position, previousRoom` signature is recovered): the source set is *every* room whose authored XZ perimeter contains the active 3-D camera transform (`Camera3d`-filtered, so a menu `Camera2d` surviving a transition frame can't add a wrong room) or the player vehicle's physics position, their decompressed lists unioned — an ambiguous or camera-lagged pick can only over-show, never hide an authored-visible room, and the room under the car survives chase-camera boundary lag; a position inside no room contributes nothing, and no sources at all bypasses culling rather than hiding the city. Implementation: `CityRoom(id)` tags each per-room render group at spawn (`rooms` index + 1 — colliders are physics, never culled); `CityPvs` (session-scoped like `CityNav`) holds the table plus decoded source lists; `apply_city_pvs` (Update, after the camera systems) re-resolves each frame and rewrites `Visibility` only on change/toggle/new-room arrival; a missing/unparseable `.cpvs` yields no resource — unculled, logged, never fabricated. Telemetry: `pvs=<room>r/<hidden>h/<tagged>` (or `pvs=off`), absent without a table. Authored-table behaviour, not a defect: gameplay-height captures are capture-noise-identical on/off while elevated viewpoints show rooms the table marked invisible — retail renders the same holes from the same table. | `mm2_app::pvs` (`CityPvs`/`CityRoom`/`apply_city_pvs`), `mm2_app::city` (per-room tagging + sibling `.cpvs` load), `docs/research/environment.md` |
| DSN-34 | Deadly-water consumption (F18-A.6/.7): the verified `.water` record and the exe's SDL marking (both halves of WLD-23) bind to a session-scoped `mm2_app::water::CityWater` — the authored level, each ref resolved to its room's XZ perimeter (`room_poly`, the same containment the PVS lookup uses) at bound `max(level, room-top)`, plus every room whose *first* attribute is a `TextureRef` to a drowning-class surface at bound `level`. Verified (from Midtown2.exe `cityLevel::Load`): both sources set the same runtime water flag and the kill bound is the one global level. Designed: the exposure rule — a point is exposed inside a marked room's authored XZ perimeter at/below its bound (the original tests room occupancy; point-in-perimeter is the same set for a contained point), `max(level, room-top)` honours mm2kiwi's "deadly water at any height" for a mod room whose plane sits above the global level (the exe itself bounds everything by the global level), and a 0.1 m slack covers contact-vs-plane float noise. The class-flag derivation is unrecovered, so `SurfaceTables::is_deadly_surface` approximates it as `drag >= water_min_drag` — the same boundary the wheel classifier uses; it reproduces the retail mark set exactly (deepwater only). `track_recovery` overlays it on the wheel-`drag` classification (F05-B.5): a grounded wheel whose contact point is exposed counts as water whatever the collider's `drag` says, and a car under a listed room's bound with no contact — clipped through the plane — accrues the submersion dwell instead of free-falling or tripping the OOB leg; a deck above the bound inside the same XZ stays dry. Missing/unparseable `.water`, a non-finite level or a build resolving to no rooms yield no resource; absent surface tables skip only the SDL pass — the material classification still covers ordinary water, so the failure direction is "no authored escalation", never a fabricated kill zone. `CityWater` is inserted by `load_session_world` and removed by `drive_session` teardown; the smoke record carries `wtr=<level>/<refs>r(+<n>sdl)(+<n>s)` only when a record loaded. | `mm2_formats::water` (`WaterDef`), `mm2_formats::psdl` (`texture_ref_index`), `mm2_content::surface` (`is_deadly_surface`), `mm2_app::water` (`CityWater`), `mm2_app::recovery` (`track_recovery`/`ground_contact`), `docs/research/environment.md` |
| DSN-35 | Vehicle audio runtime (F07-A.2): the local car's authored `aud/cardata/{player,opponent}/<id>.csv` loads with `VehicleDef` (absent/malformed audio warns and never sinks the vehicle) and binds to a session-owned `VehicleAudio`. The authored `Horn wave name` resolves through `wav::lookup_stem` (directory/extension/rate-suffix stripped, case-folded) against a session `WaveBank` census of `aud/**/*.wav` — `aud22` variants preferred over `aud11` on a stem tie (designed; AUD-2 leaves the original's pick unverified). One `ENTER` press (documented horn key, CTL-1) fires one one-shot clip — whether the original holds for the press duration or retriggers is unverified, so single-shot-per-press is the designed reading. Voices are bounded (`MAX_VOICES` 8, excess presses dropped), `PlaybackMode::Despawn` self-cleans at end-of-clip, sink-paused with the session and despawned with session teardown — voice lifecycle is an implementation policy, not an original claim. Decode reuses the bounded `wav` parser through a custom `Decodable` `PcmAudio` asset (`bevy_audio` enabled, Bevy's own WAV loader unused); oversized `data` chunks are rejected before sample materialization. Missing stems and malformed waves land in `AudioReport`/`missing` diagnostics, never silently fabricated. `--horn` is a dev evidence trigger (`DevOverrides.horn` — fires once at `Playing`, presentation-only so it stays outside `record_eligibility`). Smoke record field `aud=<presses>h/<voices>v/<sinks>s` reports demand vs spawned vs mixer-attached. | `mm2_formats::wav::lookup_stem`, `mm2_formats::cardata::CarAudio`, `mm2_content::assemble::load_car_audio`, `mm2_game::audio::VehicleAudio`, `mm2_app::audio` (`PcmAudio`/`WaveBank`/`AudioVoice`/`HornRequest`/`AudioReport`), `mm2_app::smoke` |
| DSN-36 | Engine loop rig (F07-B.1): every `Engine wave name` row in the player's cardata that resolves the canonical fade-window schema spawns one `PlaybackMode::Loop` voice parented to the car (dies with it; sits at its transform for the future spatial mix), and `engine_drive` re-mixes each loop every frame off `VehicleState.rpm` — the sim's drivetrain-derived engine RPM, already gear- and direction-aware, so shift and reverse legs fall out of one quantity rather than a throttle/world-speed read (F07 req 2). The designed formula (inferred, UNK-25 — the original's exact application is unrecovered): the fade-in window ramps an envelope `0→1`, the fade-out window ramps it `1→0`, volume interpolates `Min Volume→Max Volume` over the combined envelope so a loop outside its band is silent rather than idling at its authored minimum, and speed interpolates `Min Pitch→Max Pitch` across the `Pitch shift` window. Degenerate windows are step functions (vpbug's VWHIGH `fade out 15000,15000` = no fade inside reachable RPM, still a hard cut past the bound); a non-finite RPM silences the loop. Loops hold at volume 0 outside their band instead of churning sink attach/detach. Rows in the divisor schemas carry no RPM windows and resolve `None` — skipped with a counted warning, never guessed. The rig is player-only and non-spatial (the local car sits at the listener); opponent/ambient engines and the clutch sample's trigger stay open under UNK-25. `aud=` gains `<loops>l/<audible>a`. | `mm2_game::audio` (`EngineLoopSpec`/`EngineMix`), `mm2_app::audio` (`EngineRig`/`EngineVoice`/`engine_rigs`/`engine_drive`), `mm2_app::smoke` |
| DSN-37 | Opponent engine rigs + spatial listener (F07-B.2): opponents spawn with `VehicleAudio` — the opponent-side cardata record `load_opponent` already resolves, same absence policy as the player — and `engine_rigs` builds a rig on every `VehicleAudio` car, bounded `MAX_ENGINE_RIGS` 16 per session. Non-player loops are `PlaybackSettings::spatial` emitters (Bevy/rodio spatial: inverse-square attenuation plus left/right ear panning — an implementation policy, not an original claim) scaled by `ENGINE_SPATIAL_SCALE` 0.25, a designed pick that reads a 4 m opponent at ~full authored volume, a 5–15 m racing pack clearly audible and a 50 m straggler near silence. The player's own rig stays non-spatial — the local car anchors the mix so camera distance never fades it. `audio_listener` keeps exactly one `SpatialListener` on the `is_active` `Camera3d` (chase↔free toggle moves the ear the same frame; `Camera3d`-filtered so a transition-frame `Camera2d` can't become the listener, the same guard `apply_city_pvs` applies), and `engine_drive` applies the computed mix to `AudioSink`/`SpatialAudioSink` alike. Rig/listener entity commands order `.after(session::drive_session)` so a mid-update teardown can't queue inserts on despawned cars/cameras. `aud=` gains `<rigs>r`. The original's spatial model, attenuation curve and listener placement are unrecovered (UNK-25) — this binds Bevy's spatial sink as a designed reading, not an original-behavior claim. | `mm2_app::audio` (`audio_listener`/`ENGINE_SPATIAL_SCALE`/`MAX_ENGINE_RIGS`), `mm2_app::opponents` (`spawn_opponents`), `mm2_app::smoke` |
| DSN-38 | Impact audio (F07-B.3): the session loads the player-side `aud/cardata/player/default_impacts.csv` into an `ImpactAudio` resource (parsed `ImpactTable` + generation-seeded `NavRng`; absent/malformed warns once and yields no resource — never a fabricated category). `impact_voices` consumes the deduplicated `ImpactEvent` stream after `session::drive_session` in both schedules: stale-generation events are skipped, a non-`Playing` session drains without emitting, and each event's *vehicle* participants each earn one voice — a remote participant's sound belongs to its authority's client (the same skip every F05 consumer applies), a non-vehicle participant (knocked prop meeting the world) earns none. Designed readings, all UNK-25: the struck side's `dgBangerData.AudioId` selects the category `ID` with an id-0 (`WALL`) fallback (every retail record authors `AudioId` 0, so retail always reads `WALL` — the only selector the data names); the force weighed against `min,max force` bands is `severity × striker mass` (the `impulse_estimate` contract — `ComputedMass` → `Mass` → config mass, each checked before falling through so a not-yet-computed `Mass(0)` can't collapse the pick to a 1 kg touch); `frequency` weights the covering samples (all-zero → uniform) and `min,max volume` draws the gain; a force below every band is authored silence, not an error. The player-side table is the binding — the opponent file's `WALL` bands diverge two orders of magnitude in-column, an authored inconsistency not normalized. Voices are `PlaybackMode::Despawn` one-shots bounded `MAX_IMPACT_VOICES` 12 (pile-ups count `+Nd` drops, F07-AC04's bounded leg), `SessionEntity`-swept at teardown, spatial emitters at the impact point under `ENGINE_SPATIAL_SCALE` for everyone but the local player (non-spatial, DSN-37 anchor). Whether the original emits per car side or once per collision is unrecovered. `aud=` gains `<impacts>i` when nonzero. | `mm2_game::audio` (`impact_category`/`pick_impact`/`ImpactPick`), `mm2_game::banger` (`BangerDefinition.audio_id`), `mm2_app::audio` (`ImpactAudio`/`impact_voices`/`MAX_IMPACT_VOICES`/`IMPACT_TABLE`), `mm2_app::session`, `mm2_app::smoke` |
| DSN-39 | Surface skid/rolling audio (F07-B.4): the session loads the weather-selected player-side surface table (`default_surface<variant>.csv`, DSN-43) into a `SurfaceAudio` resource (absent/malformed warns and yields none — never a fabricated row). `surface_voices` runs after `session::drive_session` in both schedules: per grounded wheel the contact's `SurfaceMaterial` resolves through `SurfaceTables::sound_index` — `Authored(i)` reads `materials.mtl` def *i*'s `sound` field, `Unspecified` reads the `_default` block's — to a row of the table (the material-`sound`→row binding is a designed reading; `sound` is the only selector the authored data names, UNK-25). Per row, `SurfaceSpec::from_entry` resolves the canonical `max speed` schema into a `RollingSpec` (volume/pitch interpolated over `0..max speed`) and a `SkidSpec` (the `skid wave` header's unit column decides the trigger: `slippage` → the designed `tire_slippage` quantity — `max(|traction_demand|, |slip_angle|/peak_slip_angle)` clamped to 0..1, the closest "past the tire's limit" measure the arcade model publishes since it has no wheel-speed state; `speed` → the wheel's `|vel_long|`); the divisor-schema (ice) rolling loop resolves `None` rather than a guessed divisor formula. A covering `min,max` band interpolates `min,max skid volume`; below every band is authored silence. Voices are `PlaybackMode::Loop` children of the car, spawned lazily per committed entry (skid per covering band, rolling past `ROLL_MIN_SPEED` 0.5 m/s), idle at volume 0 rather than churning attach/detach, rebuilt only after a different entry holds `SURFACE_DWELL` 4 frames, bounded `MAX_SKID_VOICES` 4 bands and `MAX_SURFACE_RIGS` 16 cars, `SessionEntity`-swept at teardown, spatial for non-local cars and non-spatial for the player (DSN-37). A held brake at rest and airborne wheels resolve nothing — F07-AC03's legs are silent by construction. The weather→{dry,wet} variant binding landed in F07-B.8 (DSN-43 — `surfaceice` has no exe string, AUD-11); `Tunnel sound index` and `for tunnels` remain unconsumed. `aud=` gains `<skids>k/<rolling>g` gauges when nonzero. | `mm2_game::audio` (`SurfaceSpec`/`SkidSpec`/`RollingSpec`/`SkidUnit`/`tire_slippage`), `mm2_content::surface` (`sound_index`), `mm2_app::audio` (`SurfaceAudio`/`SurfaceRig`/`SurfaceVoice`/`surface_voices`), `mm2_app::smoke` |
| DSN-40 | Clutch one-shot on committed drivetrain changes (F07-B.5): every `VehicleAudio` car carries a `GearWatch` of the last committed `(gear, direction)` — `clutch_voices` runs `.after(session::drive_session)` in both schedules so the watch observes the gearbox step's discrete `state.gear` commits and `direction` flips, not per-frame noise. A differing committed pair plays the cardata `clutch wave name` at `clutch volume` through the session `WaveBank` — the roster dump binds `REVERSE` (~0.93, vppanoz 0.96) on the player/opponent cars and `TRUCKGEARSHIFT` (0.87) on vpbus/vpcentury/vpddbus/vpsemi, both stems resolving in `aud11`/`aud22`. The trigger is a designed reading (UNK-25 — which transitions the original voices is unrecovered): one one-shot per committed pair change, a multi-gear selector jump in one step is a single actuation, first sight inserts the watch silently (a spawned car did not "shift into" its initial gear), and the watch updates while not `Playing` so a countdown/pause change never flushes as a stale clunk (the `impact_voices` drain contract applied to state). Edge consequences of the per-frame diff, disclosed as designed: a same-frame double shift (or shift-and-shift-back) collapses to at most one voice, and a mid-`Playing` `ResetVehicle` (recovery/damage reset rewrites `VehicleState::new` → gear 0/Forward) commits a pair change and voices a clunk. A remote participant is watched but never voiced — its clutch belongs to its authority's client, the same skip every F05/F07 consumer applies; a sentinel or empty clutch name is authored silence (no `failed`); an unresolvable stem counts `failed` per change (per-event semantics like horn presses). Voices are `PlaybackMode::Despawn` `AudioVoice{Clutch}` children of the car, `SessionEntity`-stamped, spatial under `ENGINE_SPATIAL_SCALE` for non-local cars and non-spatial for the local player (DSN-37), bounded `MAX_CLUTCH_VOICES` 8 with `+Nd` bound drops; the authored volume rides the `authored_volume` sanitizer shared with the horn. `aud=` gains the activity-gated `/<n>c` clutch count; `DriveDirection` joined `mm2_vehicle`'s root re-exports (already `VehicleState::direction`'s public type). | `mm2_app::audio` (`GearWatch`/`clutch_voices`/`MAX_CLUTCH_VOICES`/`authored_volume`/`VoiceKind::Clutch`), `mm2_vehicle` (`DriveDirection`), `mm2_app::smoke` |
| DSN-41 | Ambient-traffic engine voices (F07-B.6): `class_assets` resolves each `va_*` class's `aud/cardata/ambient/<id>_engine.csv`, falling back to the authored `default_engine.csv` — a designed binding (UNK-25): the filenames are the only per-class link the data names, and only `va_bus_f`/`va_ddbus_l`/`va_diesels_s`/`va_smallsuv_s` match a stock roster id exactly (AUD-9's `va_sedan_s`/`va_garbagetruck`/`va_pickup_f` tables are dead data under it). Absent tables mean noteless cars; a malformed resolved file warns once per class and the cars stay noteless — never a silent substitution of the default over a broken per-class file. The resolved `AmbientEngineSpec` rides each spawned car as `AmbientAudio`; `ambient_engine_rigs` turns it into one `PlaybackMode::Loop` `AudioVoice{AmbientEngine}` child — always spatial under `ENGINE_SPATIAL_SCALE` (an ambient car is never the local listener anchor, so DSN-37's non-spatial rule does not apply), `SessionEntity`-swept at teardown, bounded `MAX_AMBIENT_VOICES` 32 live loops (designed; cars past it count `+Nd` once via the `AmbientRig` marker, and a recycled body re-attempts). `ambient_engine_drive` re-mixes every loop off the parent's `LinearVelocity` magnitude — the same component lane followers and knocked wrecks both publish, so a wreck's note winds down on its own — through the authored speed bands: the *first covering band in authored order* wins (retail authors tight bands ahead of a `0–500` catch-all, so authored order is the only reading under which the tight bands are reachable), an uncovered speed evaluates the nearest band at its edge (ties prefer the earlier row, so the catch-all's ~24.6 max pitch never wins over a tight band), negative velocity reads as speed magnitude, and a non-finite speed silences the loop. The authored `engine volume` is constant — the table has no speed→volume term, so a stopped car idles at the same gain; a sentinel sample or missing `AmbientAudio` is authored silence and an unresolvable stem counts `failed` once per car. Which classes the original binds which table, what quantity it feeds the speed bands (m/s assumed) and whether the original even pitches the ambient loop are all unrecovered — designed reading, UNK-25. `aud=` gains `/<n>e/<n>n` (spawned/audible) when nonzero. | `mm2_game::audio` (`AmbientEngineSpec`/`AmbientAudio`), `mm2_content` (`ambient_engine_audio`), `mm2_app::traffic` (`AmbientClass.audio`), `mm2_app::audio` (`AmbientRig`/`AmbientEngineVoice`/`ambient_engine_rigs`/`ambient_engine_drive`/`MAX_AMBIENT_VOICES`/`VoiceKind::AmbientEngine`), `mm2_app::smoke` |
| DSN-42 | Authored siren programs (F07-B.7): `load_session_world` resolves `aud/cardata/player/<psdl-stem>policesiren.csv` — the city-keyed naming the exe's `sfpolicesiren`/`londonpolicesiren` strings imply — plus the shared `aud/cardata/opponent/policesiren.csv` into a `SirenAudio` resource (generation-seeded `NavRng`; an absent or malformed file warns once and leaves that side programless — never a fabricated or cross-side substitute). A car whose horn row carries `flags & 4` (`SIREN_FLAG`, vpcop on retail — AUD-10) toggles the program on each `HornRequest` press: press-to-toggle on the horn control is the designed reading — the flag lives on the horn row and the exe carries no other siren input binding, but hold-vs-toggle vs pursuit-state is unverified (UNK-25); a flagged press never plays the authored horn sample, and unflagged cars keep the ordinary horn unchanged. `SirenPlayback` enters at sample 0, picks one step at seeded random per entry (multi-step rows as authored dwell/branch variety — designed), dwells `play time` seconds measured on the session's fixed tick (a frozen tick holds the program mid-dwell and `sync_audio_pause` holds the sink), then jumps to the sample `next index` names; a negative or out-of-range target, an empty target sample and a non-finite `dt` all end it, and `MAX_SIREN_HOPS` 16 bounds a malformed all-zero-time chain. The remote-participant skip matches every F05/F07 consumer. Each activation spawns one `PlaybackMode::Loop` `AudioVoice{Siren}` child of the car and respawns it on each authored switch (a looping sample can't be hot-swapped); voices are `SessionEntity`-stamped, spatial under `ENGINE_SPATIAL_SCALE` for non-local cars and non-spatial for the local player (DSN-37), bounded `MAX_SIRENS` 8 concurrent programs with `+Nd` drops. Stems resolve `aud/*/sirens/` first — the `sirens\%s` scope the exe names — falling back to the global `WaveBank` stem index; a sentinel name is authored silence, an unresolvable stem warns once per activation and the program keeps walking in silence. The authored `Explosion sample` binding is carried on `SirenAudio` but has no consumer (F20). `aud=` gains `/<n>w/<n>y` (voices spawned/programs live) when nonzero. | `mm2_formats::cardata` (`SirenProgram`/`SirenSample`/`SirenStep`), `mm2_game::audio` (`SirenSpec`/`SirenPlayback`/`SirenTransition`/`SIREN_FLAG`/`MAX_SIREN_HOPS`), `mm2_app::audio` (`SirenAudio`/`Siren`/`siren_toggle`/`siren_drive`/`MAX_SIRENS`/`WaveBank::load_siren`/`VoiceKind::Siren`), `mm2_app::session`, `mm2_app::smoke` |
| DSN-43 | Surface-variant weather binding (F07-B.8): `load_session_world` resolves the session's effective weather once through `mm2_game::effective_conditions` (customization > authored event params > configured defaults — the same conditions the `env=ltNN` lighting and fog bindings already consume, RACE-2/DSN-28/DSN-29) and `SurfaceAudio::load` binds `SurfaceVariant::for_weather` — `rainy` (selector 3) → `wet`, every other authored selector → `dry` (designed: the exe's `%s_surface{dry,wet}` + `default_` strings prove a runtime two-variant selection exists, AUD-11, but which state selects wet is unrecovered, UNK-25; `surfaceice` has no exe string and stays dead data). The probe order is `aud/cardata/player/<vehicle-id>_surface<variant>.csv` then `default_surface<variant>.csv` — the `%s_` format the exe carries, with the vehicle catalog id as the inferred stem (stock ships no per-vehicle file, so an absent candidate probes silently); a present-but-malformed or wrong-kind candidate warns and falls through to the `default_`, which warns and yields no `SurfaceAudio` on its own failure. No cross-variant substitution: a rainy session without a wet table gets no surface audio rather than a dry table mislabeled as wet. The bound `variant`/`path` ride the resource for diagnostics; a wet session adds ` surf=wet` to the smoke record (dry and absent emit nothing — the record is bit-identical to pre-B.8 under them). Retail legs: `sf --weather 3 --headless` reports `surf=wet` with `env=lt03(rainy-morning)` confirming the same slot; `--weather 0` reports no marker. | `mm2_game::audio` (`SurfaceVariant`), `mm2_app::audio` (`SurfaceAudio::load`/`surface_table_paths`), `mm2_app::session`, `mm2_app::smoke` |
| DSN-44 | Gate-coverage constraint on `.opp` densification (F15-B.6 extension): `NavGraph::densify_route` takes the race's checkpoint list and rejects, per leg, any re-path that drops a trigger the authored segment crossed — `Checkpoint::crossed` applied to the authored a→b segment and to every consecutive pair of the driven chain (a → lane samples → b). A rejected re-path keeps the authored leg verbatim, the same fallback unroutable legs already used. Authored lines legitimately leave the routable network at gates the BAI does not cover (measured `london circuit:6`: gate 0 sits on a flyover whose ramp mouth is dressed with breakable construction props — the authored anchor line crosses the cylinder at 4.7 m inside r10 while the lane-following detour missed by 198 m and stalled the whole Ordered field at cp 0). Re-paths are still taken whenever they preserve coverage — including ones that newly cross a gate the authored segment missed (that bonus coverage stands; it fixes courses whose own line strays from a trigger). Empty `gates` preserves the previous behaviour. Implementation choice: no original rule couples `.opp` densification to checkpoints because the original never densifies — the route itself is the AI course (UNK-11). | `mm2_game::nav` (`densify_route`), `mm2_app::opponents` (`driving_route`), `mm2_app::session` |

## Open questions (unknown until evidenced)

| ID | Question |
| --- | --- |
| UNK-1 | Exact enum maps for `CarType`, `TimeofDay`, `Weather`, `Difficulty` columns (0-3 seen; no key shipped). Narrowed 2026-09-22: `Weather`/`TimeofDay` order is now measured (WLD-21) — `ltNN = tod×4 + weather`, tod = morning/noon/evening/night, weather = clear/cloudy/foggy/rainy. `CarType`/`Difficulty` order still unknown. |
| UNK-2 | Whether `.aimap` files beyond the `mm*data.csv` rosters (e.g. london race12-13, blitz10-12, circuit10-11, sf r0) are selectable events, variants or leftovers. Both cities' `circuit11` lack `.aimap` entirely. |
| UNK-3 | How `vpmoonrover` is unlocked/selected in the original (undocumented; likely cheat code — no verified source). |
| UNK-4 | Blitz `TimeLimit` unit — the runtime binds it as seconds provisionally (DSN-7); the constant 50/40 `TimeLimit` on checkpoint/circuit rows is likely unused — unconfirmed. |
| UNK-5 | `NumLaps` nonzero on non-circuit tables — likely an unused shared column; unconfirmed. |
| UNK-6 | `.info` `Flags`/`UnlockFlags` bit meanings and whether `UnlockScore` gates via Pro points. |
| UNK-7 | Whether race names live in `mmlang.dll` string tables (`.info` descriptions exist for cars; race Description fields are `none`/lesson ids). |
| UNK-8 | Pro-points scoring formula (inputs documented; weights unknown). |
| UNK-9 | Police pursuit AI, sight model, spawn/de-spawn rules beyond "fixed spots" + "line of sight". |
| UNK-10 | C&R gold spawn/hideout positions, scoring values, drop mechanics, respawn timing. |
| UNK-11 | Opponent AI route choice/difficulty model. The roster side is now verified (RACE-11/RACE-12): which aimap variant binds, which vehicle/route each row wires, and how the wired count relates to the table `Opponents` column. Partially measured: the `.opp` `brake` column is mislabelled — a nonzero value marks a staging record whose payload is a heading in vehicle-yaw degrees (row 0 on 592/612 retail files, 542 agreeing with route course direction within ~25°; `race/sf/race5-a-{5,6,7}` carry a second staging row mid-file whose trigger is unknown). Still unverified: the remaining `.opp` columns (`forward/side offset`, `target speed`, `speed/side start` — authored zero on all retail rows, preserved raw; the runtime treats `.opp` as a polyline plus staging), the exact original consumption of the `[Opponent]` parameter tail (the column *order* is now documented — R3's published table corroborated by mm2hook's `RegisterRoute` signature, RACE-14 — but how the original consumed each field is unverified: our look-ahead-as-corridor-reach and the avoid-flag gates are designed readings of the documented names, and `cornerBrakingThreshold`'s documented brake-demand floor has no consumed equivalent yet), how the original consumes the driving line vs its own route choice (CHK-5 says opponents take different routes), how the original decodes the `_opp` tuning files' alternate Trans schema and merges them with the base tune (RACE-13 — our sparse-overlay merge is a designed policy), how opponents take grid slots (UNK-17), what the spare `.opp` files were for, and whether the original applies any catch-up/rubber-band assistance to trailing opponents (a disclosed bounded policy is implemented, DSN-27 — no original semantics verified). |
| UNK-12 | Traffic/pedestrian ambient models (lanes, lights, panic reactions). `.bai` parses and builds a directed nav graph (`docs/research/bai.md`), `.aimap` overrides parse (`docs/research/aimap.md`) — how the original runtime consumes those remains unverified. `.pathset` placements parse and `city/<city>/{props,decals}.pathset` now stamp ambient prop rows (WLD-13) and decal ribbons (WLD-18); audio/race pathset consumption is still unimplemented. Runtime note (F10-A.1): the authored side of ambient traffic is now measured (WLD-20) — `.aivehicledata` tuning decodes, `[Ambient Types/Density]` cumulative rosters import per city and per event — and `mm2_game::traffic::plan_ambient` plans seeded spawns over routable lanes honouring `NavOverrides` closures. Still unverified: the original's active-pool size, spawn ring/despawn distances, per-lane quotas, ambient flag semantics, and whether density gates count or rate. Runtime note (F10-B.2): the BAI `vehicleRule` road-end codes are now consumed — `mm2_game::traffic::Junctions` gates each lane-end transfer by the authored approach rule: `TrafficLight` approaches wait for their road's phase in a deterministic per-junction member cycle (documented "one road at a time"; signal membership measured — docs/research/bai.md), `StopSign` approaches pass a first-come-first-served queue after a dwell (documented "longest waiting vehicle drives first"), `AlwaysStop` never opens, `NeverStop` flows. All timing/distance values (phase length, all-red clearance, dwell, stop-line inset, approach ramp, entry clearance) are designed — the original's are unverified. Runtime note (F10-B.6): ambient cars now carry authored `Mass`/CG and a kinematic→dynamic collision handover exists — a `CollisionStart` contact whose `approach_speed × striker_mass` estimate reaches the designed `KnockPolicy::min_impulse` (4000 N·s) converts the same entity to `RigidBody::Dynamic`, drops it from lane driving/FCFS, and leaves a solver-owned wreck that still occupies the junction box until the distance recycler collects it. The threshold, the linear impulse estimate (banger activation instead gates on striker kinetic energy `½·m·v²` since F04-C.5, DSN-10), the bounded contact-normal velocity kick, and the wreck-persistence policy are all designed — the original's ambient crash rules (do ambient cars ever deform, shove, or despawn on impact?) are unverified. Runtime note (F10-B.7): the authored `trafficLightOrigin`/`trafficLightAxis` road-end pairs now surface as session-owned indicators — `NavGraph::signals` carries every nonzero origin on a resolved end (828 London, 647 SF after 3 authored >60 m outliers drop on a designed sanity bound; ~47% of London's heads govern ends no vehicle arc approaches, so arc-exit-only traversal would miss them). `drive_signals` maps each head's aspect off `Junctions` — green while the end's road holds the phase, red otherwise/in all-red, constant green on free-flow or non-member ends, a stop aspect on stop-signed ends. The indicator geometry (unlit sphere at the authored anchor), the 60 m bound, and the aspect mapping are all designed — the original's signal-unit draw (no `vasignalunit`/`vastopunit` geometry ships, textures only) and exact state semantics remain unverified; the authored positions and rules are original data. Runtime note (F10-B.8): the ambient population now lives in the union of player interest areas — `in_spawn_band`/`within_interest` replace the single-player bubble for both the plan draw and the runtime recycler: a car is collected only past `recycle_distance` of *every* `Player` participant (local driver, remote drivers, AI opponents — designed composition: each can hold corridor/box/collision interactions with ambient cars), and a spawn must land inside at least one area's band while staying outside every area's `min_player_distance`. The original's interest-area model (per-player bubbles? AI-held areas?) is unverified; the spec's union requirement and the distance constants remain designed. Runtime note (F10-B.9): lane transfers now cross the junction interior instead of teleporting between lane extremities — `NavGraph::crossing_path` generates a cubic Hermite from the approach lane's travel-direction end to the exit lane's start, and `LaneCursor` traverses it incrementally while keeping the approach lane identity (junction occupancy/right-of-way preserved; `LaneAdvance::Entered`/`Landed` mark commit/landing). The generated geometry, its chord-scaled tangents and the resample step are designed — BAI carries no authored junction-interior curves and the original's runtime crossing path is unrecovered. The same census surfaced an authored-data quirk now handled at build: several SF lane curves are vertex-reversed relative to section order (authoring artefact, not a direction marker — `docs/research/bai.md`), so `NavGraph::build` normalizes curve order before sampling. A pose-continuity watchdog (`AmbientTraffic::jumps`, `jmp=` in the headless record) counts per-update teleport-like moves; retail headless runs report `crx=59/68 jmp=0` on sf/london. |
| UNK-13 | Exact damage accumulation model and breakaway-part rules (DMG-3 documented only as a feature claim). Narrowed 2026-09-22: the authored-data side is now measured (DMG-5/6/7/8, REC-1) — `vehCarDamage`/`vehStuck`/`vehGyro` decode fully and the break-part inventory is cross-checked. Still unverified: what quantity `MaxDamage` integrates and whether `ImpactThreshold` compares the same unit (the implemented `approach_speed × striker_mass` estimate is a disclosed designed policy), whether `MedDamage` gates visual state, impairment or both (both implemented legs key on it as designed policy — smoke DSN-24, engine impairment DSN-25; MM2Hook's `PhysicalEngineDamage` ini option documents smoke↔engine-torque coupling in the original — shape/values unrecovered), what quantity the original compares each part's `ImpulseLimit2` against and whether detachment is damage- or impact-driven (an impact-driven reading against `severity × part_mass` — each part's own record `Mass` — is implemented, DSN-21 — including whether the record's `CG`/`Size` anchors are car-space on vehicle fragments), `ApplyBirdPoopDamage`'s splat shape and the `ImpactsTable`'s fill rules (the radius test, barycentric pick and `Reset` are recovered and implemented — the radial blit is designed, DSN-32), the original's `asLineSparks` burst semantics and texture binding (a designed radial-rebound policy is implemented, DSN-26), `DoublePivot`/`MirrorPivot`/`Color`/`Height`/`Intensity` semantics beyond the implemented designed readings (DSN-24), the `.vehstuck` test combination (a designed interpretation is implemented, DSN-20 — `Rotation`/`Translation`'s roles unrecovered), `.vehgyro`'s application semantics (a designed reading is implemented — latched rate-actuator spins, drift damper relief, per-axis airborne righting, DSN-22 — while the original's trigger tests and mechanism stay unrecovered), and the original's water/out-of-bounds recovery rules (no tune-side records found — DMG-2 covers destruction only; the city-side `city/<stem>.water` record is now consumed under a designed room-scoped reading, DSN-34, while a designed dry-anchor + dwell/margin detector carries the detector itself, DSN-23). |
| UNK-14 | Crash Course pass/fail criteria per lesson (time? gates? stunts scored how?). |
| UNK-15 | Whether pedestrians can be struck and what the consequence is. |
| UNK-16 | Narrowed 2026-09-21: the two `a` columns' conventions are now measured (WPT-4) — waypoint `a` is a course bearing, `_strtpnts` `a`/`.opp` staging is vehicle yaw, 180° apart. Still unknown: whether the original enforces gate *direction* (e.g. rejects a backwards crossing) or uses waypoint `a` at runtime at all. |
| UNK-17 | `_strtpnts` row→participant mapping and which authored start set the original consumes per participant: row 0 is treated as the player slot (inferred, WPT-3), opponents take `index + 1` on a grid or the `.opp` row-0 staging point without one — both provisional policies, not verified original behavior. |
| UNK-18 | Which road-id space the over-range london `.aimap` `[Exceptions]` ids (562–815 vs 540 `city/london.bai` roads) address — `london_sup.bai`'s layout is unparsed, so this stays open (WLD-8). |
| UNK-19 | BAI `edgeDistances` semantics — not a monotone outer-edge ordering (profiles like `[7.5, 2.5, 2.5, 7.5]` on one-way sides). The nav graph ranks lanes by measured lateral offset instead (WLD-9). |
| UNK-20 | Pathset per-point `attributes` word, the `OPEN:`/`inactive:`/`open:` name prefixes (event-state decorations on bridge/gate props — inferred), and what the three truncated london files (`blitz10/11`, `london_bridge_blitz10.pathset`) were meant to place. `current_path`/`selection` are inferred dev-tool cursors, preserved raw. Runtime stamping details are likewise unverified: whether line-strip spacing restarts per segment or runs continuously (per-segment implemented), whether the final vertex is capped (it is), and what zero spacing means (one prop per vertex implemented). (The directed/line-strip yaw axis is no longer open: measured 2026-09-21, the painted direction is the image of local +X — sf `sp_lightstreet_rt_f` kerb rows put all 85 sampled +Z lamp arms over a carriageway only under that reading, and `sp_barricadeconcl_*_f` wall segments form continuous barriers; see `docs/research/pathset.md`.) Decal ribbon rendering is likewise unverified: `u` across the pair / `v` along the centre line tiled per `spacing` (5 m default) is inferred from texture contents, palette-alpha blending is inferred from authored alpha values, and whether prop-channel decal names stamp is unknown (the 31 sf `props.pathset` rail copies — 26 byte-identical duplicates — are treated as authoring leftovers). |
| UNK-21 | Prop-rule runtime field semantics (WLD-14/17): the stamping *geometry* is verified — which arcs `n{NN}left`/`n{NN}right` apply to is solved (WLD-17) and the implemented walk resolves every reachable rule room on both cities. Still unknown: whether `start`/`distance`/`maxUse` budget per room-side or per whole path (implemented per room-side), how `file1`–`file4` variants are chosen (deterministic hash implemented; the original's seed is unrecovered), what `minLerp`/`maxLerp` lerp (implemented as curb→outer midpoint; always equal on retail), the original's left/right label convention, the encoded non-room `road_rooms` record kind (65 0xx on sf), why 52 sf / 5 london rule-bearing rooms are reached by no path, and what consumes the `props.csv` `Races` group. (Prop yaw narrowed 2026-09-21: retail kerb props are authored front/arm-first along local −X — measured on lamp/mast arms, bench and sign faces — so the implemented walk yaws +X to the per-stamp kerb→building-line direction and the face lands on the carriageway; whether the original composes the identical basis remains inferred — the format carries no orientation field. See `docs/research/proprules.md`.) |
| UNK-22 | Banger runtime semantics (WLD-15/16): what `ImpulseLimit2` is compared against and what crossing it does (break vs. tip vs. nothing) — the implemented striker-kinetic-energy `½·m·v²` estimate is a provisional stand-in (DSN-10), adopted because the placed-prop census shows `ImpulseLimit2 ≈ Mass × {31.25…85342}`, a ladder only a speed-squared quantity reads coherently (F04-C.5, operator report 4 item 3); whether `NumParts` bounds spawned fragments or only echoes the PKG's BREAK count, whether fragments spawn at the activation edge or at a later break threshold (implemented: at activation, one `ImpulseLimit2` gate for both), the `ColliderId`/`AudioId`/`TexNumber` id spaces, `SpinAxis`/`BillFlags`/`Flash`/`YRadius` semantics, the `type: a` tag, when `BirthRule` fires relative to the dormant→active transition, how `default.dgbangerdata` fallback selection works, how a stamped placement acquires `INST_BANGER`, the exact dormant→active→hit/despawn transition conditions, the recovered `Timer` despawn (unimplemented — the slice settles instead) and the ×32 active-pool reclaim order, and whether the 4 fragment records carrying `NumParts>0` mean fragments-of-fragments or inert leftovers. The record↔geometry link, placement binding and R4's recovered class structure are verified; the state machine's thresholds are not. |
| UNK-23 | Surface-material runtime semantics (WLD-19): which consumer performs the texture→`materials.csv`→`materials.mtl` lookup for each surface class (PSDL rooms, PKG bound materials, INST/banger colliders), what material an unmapped texture name gets (`_default` vs none — 5 authored PSDL names and `sliver` have no row), what the two dead refs (`ash`, `mud`) resolve to, the `sound` class table behind values 0/1/2, the `width`/`height`/`depth` volume semantics, the `ptxindex`/`ptxthreshold` particle index space, whether `effect` ever differs from `none`, how `friction`/`elasticity`/`drag` combine with tire parameters in the original force path, and whether the `<stem>-NNNN` frame→base lookup matches original name normalization or the original strips suffixes differently. `dirt`/`wood` definitions exist but no csv row references them — their entry point (bound materials? fallback?) is unverified. Runtime note (F06-A): the implemented importer classifies PSDL colliders by authored material index and gives `none`/blank/unmapped/dead-ref slots `SurfaceMaterial::Unspecified` — a documented conservative *implementation* policy, not an answer to the `_default` question above. Runtime note (F06-B): the tire path consumes `friction` normalized against the `_default` block (`_default` → 1.0; retail water ≈ 0.76, deepwater ≈ 0.72), multiplied by a separate environment term (`TireConditions`, `--traction` dev override pending F18) — an *implementation* scaling, not a recovered original rule. Runtime note (F06-B.2): `drag` is consumed raw as a per-wheel viscous wading resistance (`drag × load` opposing contact-plane motion — only water/deepwater carry it on retail, so a car crawls ~1 m/s on the Thames surface while dry land is unchanged) and `elasticity` is consumed as the collider's Avian restitution scaled ×0.1 (the same conservative cap `convert` applies to `BoundElasticity`) — both *implementation* mappings, not recovered original formulas. Runtime note (F07-B.4): `sound` now has a consumer — `SurfaceTables::sound_index` reads it as the positional index into the weather-bound surface table (DSN-39/DSN-43) for the wheel-contact skid/rolling rig, a designed binding under UNK-25. `effect`, `width`/`height`/`depth` and `ptx*` still have no consumer. |

| UNK-24 | Environment/weather runtime semantics (WLD-21/22/23/24): the authored data now parses fully — `.ltNN` light vectors/ambients, `_fog.csv` per-preset fog tables, `.sky` dome fields, `.cpvs` 2-bit visibility, `.pvshist` weights, `.lmap` values, `.water` refs — and `.ltNN` (DSN-28), `_fog.csv` (DSN-30), `.sky` (DSN-31) and the base `.cpvs` (DSN-33) now bind to Bevy session lighting/fog/dome/room culling: slot map/directions/colours/ambient/fog band/paint-job texture verbatim, absolute intensity constants, the `FogFalloff::Linear` curve and the dome's transform readings designed. The original's true application semantics stay unverified: per-channel diffuse vs any intensity term, the exact fog curve the `FogNearClip`/`FogFarClip` fields interpolate (linear inferred from fixed-function conventions, not recovered), and whether `HatYOffset`/`YMultiplier`/`RotationRate` compose the dome transform as named (bound as world-height / vertical squash / radians-per-second — designed readings, plus the designed camera-centring horizon policy and 900 m radius). Resolved 2026-09-23 against the retail exe: the `.cpvs` variants are offline bake sweeps (the loader opens only `city/<stem>.cpvs`), `.pvshist` has no exe reference (bake-tool data), `.lmap` IS runtime-loaded (`wrong lightmap version`/`room count mismatch` diagnostics — value semantics still open), and `.water`'s consumer is verified — refs and SDL first-attribute `TextureRef`s to drowning-class surfaces both set the runtime water flag (mm2hook `RoomFlags::Water`), kill test `flagged && pos.y < level`; the class-flag derivation stays unrecovered (`is_deadly_surface` approximates it by drag, DSN-34). Still open: `.lmap` value semantics (the −267 body value and the 0xCDCDCDCD sentinel), `.ldef` integer-pair semantics (milli-radian heading/pitch is a guess), the `amb_*`/`sky_*` letter-grid meanings (w≈clear/foggy/partly-cloudy/rainy inferred; t/v unverified), whether the original fogs or lights its dome (bound unlit + fog-exempt — designed), `sf_fog_orig.csv`'s role (earlier revision read), and how `timeOfDay`/`weather` indices map onto `.ltNN` slots beyond the measured name grid. |
| UNK-25 | Audio runtime semantics (AUD-1…9): every `aud/**` file is classified and the wave/cardata grammars parse on retail; F07-A.2 binds the authored horn to runtime playback (DSN-35), F07-B.1 drives the engine fade windows off the sim's drivetrain RPM (DSN-36), F07-B.2 extends rigs to opponents as spatial emitters heard through the active camera (DSN-37) and F07-B.3 consumes the deduplicated impact stream through the player-side `default_impacts.csv` (DSN-38), F07-B.4 drives skid/rolling loops off wheel contacts through the weather-bound `default_surface{dry,wet}.csv` (DSN-39/DSN-43) and F07-B.5 voices the authored clutch on committed `(gear, direction)` changes (DSN-40), F07-B.6 voices ambient-traffic engines through the resolved `*_engine.csv`/`default_engine.csv` tables (DSN-41) but the wider application rules stay unverified — the original's spatial/attenuation model and listener placement included — whether the original computes the same fade/pitch formula the designed reading uses (and whether it consumes a different RPM quantity), the clutch wave name's trigger — a designed committed-`(gear, direction)`-change trigger implemented (DSN-40); which transitions the original voices stays unverified, what the `Volume divisor`/`Pitch divisor`/`vol inverse RPM` compact schema computes, the `Tunnel sound index` values (0 vs 5 — the player wet file's 5 is an authored quirk), the surface-table runtime semantics — which session state selects wet vs dry (the exe carries only the `%s_`/`default_` `surface{dry,wet}` strings; a designed `rainy`→wet binding is implemented, DSN-43, and `surfaceice` has no runtime reference at all — AUD-11), what quantity the original feeds the slippage vs speed band units (designed `tire_slippage`/`|vel_long|` implemented), and the divisor-schema rolling formula (unresolved — no `max speed` window to read), the original's `default_impacts` semantics — category↔banger binding, the weighed force quantity and per-side emission (designed readings implemented: `AudioId`→`ID` with id-0 fallback, `severity × striker mass`, one voice per vehicle participant — DSN-38), the horn-row `flags` word's non-siren bits (`1`/`2`/`8` on vpbus/vpcentury/vpddbus/vpsemi — AUD-10; bit `4` now binds the siren program, DSN-42) and whether the original holds or retriggers the horn for a press (implemented one clip per press — designed), the ambient `*_engine.csv` bindings and speed-band application — a designed per-class-file-then-`default_engine.csv` resolution, `LinearVelocity`-magnitude input and first-covering-band pitch reading implemented (DSN-41); which classes the original binds and what it feeds the bands stays unverified, the siren program's runtime semantics — press-to-toggle on the horn control, per-entry random step selection and loop-per-sample voices are designed readings (DSN-42); the original's trigger (hold vs toggle vs pursuit state), how it picks among a sample's authored steps, the `Explosion sample` consumer and whether a program ever terminates (all retail programs cycle) stay unverified, whether `aud11` variants serve non-speech refs (runtime prefers `aud22`, siren stems the `aud/*/sirens/` subtree — designed), `spchdata`/`creaturedata` cue grammars, and all DirectMusic playback (`DMSG`/`DMST`/`DLS `/`DMBD` recognized by form only). |

## Gaps in this ledger

- `MM2HELP.HLP` topics on joystick/gamepad/wheel button maps and
  serial/modem setup are documented but not yet transcribed into rules;
  they matter for F23, not yet needed.
- No observable-behavior verification yet (no original executable run,
  no packet capture): everything marked *documented* is doc-trust only.
- Race *names* shown in the UI are unverified (UNK-7) — the CSV
  `Description` column is `none` for all race events.
